---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    mesh.envoy.io/proto-source: envoy.config.listener.v3.Listener
  name: listeners.mesh.envoy.io
spec:
  group: mesh.envoy.io
  names:
    kind: Listener
    listKind: ListenerList
    plural: listeners
    singular: listener
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: '[#next-free-field: 38]'
            properties:
              access_log:
                description: Configuration for :ref:`access logs <arch_overview_access_logs>`  emitted
                  by this listener.
                items:
                  properties:
                    filter:
                      description: Filter which is used to determine if the access
                        log needs to be written.
                      properties:
                        and_filter:
                          description: And filter.
                          properties:
                            filters:
                              items:
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              type: array
                          type: object
                        duration_filter:
                          description: Duration filter.
                          properties:
                            comparison:
                              description: Comparison.
                              properties:
                                op:
                                  description: Comparison operator.
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                value:
                                  description: Value to compare against.
                                  properties:
                                    default_value:
                                      description: Default value if runtime value
                                        is not available.
                                      format: int32
                                      type: integer
                                    runtime_key:
                                      description: Runtime key to get value for comparison.
                                        This value is used if defined.
                                      type: string
                                  type: object
                              type: object
                          type: object
                        extension_filter:
                          description: 'Extension filter.  [#extension-category: envoy.access_loggers.extension_filters]'
                          properties:
                            name:
                              description: The name of the filter implementation to
                                instantiate. The name must  match a statically registered
                                filter.
                              type: string
                            typed_config:
                              description: 'The type in this field determines the
                                type of API listener. At present, the following  types
                                are supported:  envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                                (HTTP)  envoy.extensions.filters.network.http_connection_manager.v3.EnvoyMobileHttpConnectionManager
                                (HTTP)  [#next-major-version: In the v3 API, replace
                                this Any field with a oneof containing the  specific
                                config message for each type of API listener. We could
                                not do this in v2 because  it would have caused circular
                                dependencies for go protos: lds.proto depends on this
                                file,  and http_connection_manager.proto depends on
                                rds.proto, which is in the same directory as  lds.proto,
                                so lds.proto cannot depend on this file.]'
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          type: object
                        grpc_status_filter:
                          description: gRPC status filter.
                          properties:
                            exclude:
                              description: If included and set to true, the filter
                                will instead block all responses  with a gRPC status
                                or inferred gRPC status enumerated in statuses, and  allow
                                all other responses.
                              type: boolean
                            statuses:
                              description: Logs only responses that have any one of
                                the gRPC statuses in this field.
                              items:
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              type: array
                          type: object
                        header_filter:
                          description: Header filter.
                          properties:
                            header:
                              description: Only requests with a header which matches
                                the specified HeaderMatcher will  pass the filter
                                check.
                              properties:
                                contains_match:
                                  description: 'If specified, header match will be
                                    performed based on whether the header value contains  the
                                    given value or not.   .. note::     Empty contains
                                    match is not allowed. Please use ``present_match``
                                    instead.   .. attention::     This field is deprecated.
                                    Please use :ref:`string_match <envoy_v3_api_field_config.route.v3.HeaderMatcher.string_match>`.   Examples:   *
                                    The value ``abcd`` matches the value ``xyzabcdpqr``,
                                    but not for ``xyzbcdpqr``.'
                                  type: string
                                exact_match:
                                  description: 'If specified, header match will be
                                    performed based on the value of the header.   ..
                                    attention::     This field is deprecated. Please
                                    use :ref:`string_match <envoy_v3_api_field_config.route.v3.HeaderMatcher.string_match>`.'
                                  type: string
                                invert_match:
                                  description: 'If specified, the match result will
                                    be inverted before checking.   Defaults to ``false``.   Examples:   *
                                    The regex ``\d{3}`` does not match the value ``1234``,
                                    so it will match when inverted.  * The range [-10,0)
                                    will match the value -1, so it will not match
                                    when inverted.'
                                  type: boolean
                                name:
                                  description: Specifies the name of the header in
                                    the request.
                                  type: string
                                prefix_match:
                                  description: 'If specified, header match will be
                                    performed based on the prefix of the header value.   ..
                                    note::     Empty prefix is not allowed. Please
                                    use ``present_match`` instead.   .. attention::     This
                                    field is deprecated. Please use :ref:`string_match
                                    <envoy_v3_api_field_config.route.v3.HeaderMatcher.string_match>`.   Examples:   *
                                    The prefix ``abcd`` matches the value ``abcdxyz``,
                                    but not for ``abcxyz``.'
                                  type: string
                                present_match:
                                  description: If specified as true, header match
                                    will be performed based on whether the header
                                    is in the  request. If specified as false, header
                                    match will be performed based on whether the header
                                    is absent.
                                  type: boolean
                                range_match:
                                  description: 'If specified, header match will be
                                    performed based on range.  The rule will match
                                    if the request header value is within this range.  The
                                    entire request header value must represent an
                                    integer in base 10 notation: consisting of  an
                                    optional plus or minus sign followed by a sequence
                                    of digits. The rule will not match if  the header
                                    value does not represent an integer. Match will
                                    fail for empty values, floating  point numbers
                                    or if only a subsequence of the header value is
                                    an integer.   Examples:   * For range [-10,0),
                                    route will match for header value -1, but not
                                    for 0, ``somestring``, 10.9,    ``-1somestring``'
                                  properties:
                                    end:
                                      description: end of the range (exclusive)
                                      format: int64
                                      type: integer
                                    start:
                                      description: start of the range (inclusive)
                                      format: int64
                                      type: integer
                                  type: object
                                safe_regex_match:
                                  description: 'If specified, this regex string is
                                    a regular expression rule which implies the entire
                                    request  header value must match the regex. The
                                    rule will not match if only a subsequence of the  request
                                    header value matches the regex.   .. attention::     This
                                    field is deprecated. Please use :ref:`string_match
                                    <envoy_v3_api_field_config.route.v3.HeaderMatcher.string_match>`.'
                                  properties:
                                    google_re2:
                                      description: Google's RE2 regex engine.
                                      type: object
                                      x-kubernetes-preserve-unknown-fields: true
                                    regex:
                                      description: The regex match string. The string
                                        must be supported by the configured engine.
                                        The regex is matched  against the full string,
                                        not as a partial match.
                                      type: string
                                  type: object
                                string_match:
                                  description: If specified, header match will be
                                    performed based on the string match of the header
                                    value.
                                  properties:
                                    contains:
                                      description: 'The input string must have the
                                        substring specified here.   .. note::    Empty
                                        contains match is not allowed, please use
                                        ``safe_regex`` instead.   Examples:   * ``abc``
                                        matches the value ``xyz.abc.def``'
                                      type: string
                                    custom:
                                      description: 'Use an extension as the matcher
                                        type.  [#extension-category: envoy.string_matcher]'
                                      type: object
                                      x-kubernetes-preserve-unknown-fields: true
                                    exact:
                                      description: 'The input string must match exactly
                                        the string specified here.   Examples:   *
                                        ``abc`` only matches the value ``abc``.'
                                      type: string
                                    ignore_case:
                                      description: If ``true``, indicates the exact/prefix/suffix/contains
                                        matching should be case insensitive. This  has
                                        no effect for the ``safe_regex`` match.  For
                                        example, the matcher ``data`` will match both
                                        input string ``Data`` and ``data`` if this
                                        option  is set to ``true``.
                                      type: boolean
                                    prefix:
                                      description: 'The input string must have the
                                        prefix specified here.   .. note::    Empty
                                        prefix match is not allowed, please use ``safe_regex``
                                        instead.   Examples:   * ``abc`` matches the
                                        value ``abc.xyz``'
                                      type: string
                                    safe_regex:
                                      description: The input string must match the
                                        regular expression specified here.
                                      properties:
                                        google_re2:
                                          description: Google's RE2 regex engine.
                                          type: object
                                          x-kubernetes-preserve-unknown-fields: true
                                        regex:
                                          description: The regex match string. The
                                            string must be supported by the configured
                                            engine. The regex is matched  against
                                            the full string, not as a partial match.
                                          type: string
                                      type: object
                                    suffix:
                                      description: 'The input string must have the
                                        suffix specified here.   .. note::    Empty
                                        suffix match is not allowed, please use ``safe_regex``
                                        instead.   Examples:   * ``abc`` matches the
                                        value ``xyz.abc``'
                                      type: string
                                  type: object
                                suffix_match:
                                  description: 'If specified, header match will be
                                    performed based on the suffix of the header value.   ..
                                    note::     Empty suffix is not allowed. Please
                                    use ``present_match`` instead.   .. attention::     This
                                    field is deprecated. Please use :ref:`string_match
                                    <envoy_v3_api_field_config.route.v3.HeaderMatcher.string_match>`.   Examples:   *
                                    The suffix ``abcd`` matches the value ``xyzabcd``,
                                    but not for ``xyzbcd``.'
                                  type: string
                                treat_missing_header_as_empty:
                                  description: 'If specified, for any header match
                                    rule, if the header match rule specified header  does
                                    not exist, this header value will be treated as
                                    empty.   Defaults to ``false``.   Examples:   *
                                    The header match rule specified header "header1"
                                    to range match of [0, 10],    :ref:`invert_match
                                    <envoy_v3_api_field_config.route.v3.HeaderMatcher.invert_match>`    is
                                    set to true and :ref:`treat_missing_header_as_empty
                                    <envoy_v3_api_field_config.route.v3.HeaderMatcher.treat_missing_header_as_empty>`    is
                                    set to true; The "header1" header is not present.
                                    The match rule will    treat the "header1" as
                                    an empty header. The empty header does not match
                                    the range,    so it will match when inverted.  *
                                    The header match rule specified header "header2"
                                    to range match of [0, 10],    :ref:`invert_match
                                    <envoy_v3_api_field_config.route.v3.HeaderMatcher.invert_match>`    is
                                    set to true and :ref:`treat_missing_header_as_empty
                                    <envoy_v3_api_field_config.route.v3.HeaderMatcher.treat_missing_header_as_empty>`    is
                                    set to false; The "header2" header is not present
                                    and the header    matcher rule for "header2" will
                                    be ignored so it will not match.  * The header
                                    match rule specified header "header3" to a string
                                    regex match    ``^$`` which means an empty string,
                                    and    :ref:`treat_missing_header_as_empty <envoy_v3_api_field_config.route.v3.HeaderMatcher.treat_missing_header_as_empty>`    is
                                    set to true; The "header3" header is not present.    The
                                    match rule will treat the "header3" header as
                                    an empty header so it will match.  * The header
                                    match rule specified header "header4" to a string
                                    regex match    ``^$`` which means an empty string,
                                    and    :ref:`treat_missing_header_as_empty <envoy_v3_api_field_config.route.v3.HeaderMatcher.treat_missing_header_as_empty>`    is
                                    set to false; The "header4" header is not present.    The
                                    match rule for "header4" will be ignored so it
                                    will not match.'
                                  type: boolean
                              type: object
                          type: object
                        log_type_filter:
                          description: Log Type Filter
                          properties:
                            exclude:
                              description: If this field is set to true, the filter
                                will instead block all records  with a access log
                                type in types field, and allow all other records.
                              type: boolean
                            types:
                              description: Logs only records which their type is one
                                of the types defined in this field.
                              items:
                                enum:
                                - NotSet
                                - TcpUpstreamConnected
                                - TcpPeriodic
                                - TcpConnectionEnd
                                - DownstreamStart
                                - DownstreamPeriodic
                                - DownstreamEnd
                                - UpstreamPoolReady
                                - UpstreamPeriodic
                                - UpstreamEnd
                                - DownstreamTunnelSuccessfullyEstablished
                                - UdpTunnelUpstreamConnected
                                - UdpPeriodic
                                - UdpSessionEnd
                                type: string
                              type: array
                          type: object
                        metadata_filter:
                          description: Metadata Filter
                          properties:
                            match_if_key_not_found:
                              description: 'Default result if the key does not exist
                                in dynamic metadata: if unset or  true, then log;
                                if false, then don''t log.'
                              type: boolean
                            matcher:
                              description: Matcher to check metadata for specified
                                value. For example, to match on the  access_log_hint
                                metadata, set the filter to "envoy.common" and the
                                path to  "access_log_hint", and the value to "true".
                              properties:
                                filter:
                                  description: The filter name to retrieve the ``Struct``
                                    from the ``Metadata``.
                                  type: string
                                invert:
                                  description: If true, the match result will be inverted.
                                  type: boolean
                                path:
                                  description: The path to retrieve the ``Value``
                                    from the ``Struct``.
                                  items:
                                    type: object
                                    x-kubernetes-preserve-unknown-fields: true
                                  type: array
                                value:
                                  description: The ``MetadataMatcher`` is matched
                                    if the value retrieved by path is matched to this
                                    value.
                                  properties:
                                    bool_match:
                                      description: If specified, a match occurs if
                                        and only if the target value is a bool value
                                        and is equal  to this field.
                                      type: boolean
                                    double_match:
                                      description: If specified, a match occurs if
                                        and only if the target value is a double value
                                        and is  matched to this field.
                                      properties:
                                        exact:
                                          description: If specified, the input double
                                            value must be equal to the value specified
                                            here.
                                          format: double
                                          type: number
                                        range:
                                          description: 'If specified, the input double
                                            value must be in the range specified here.  Note:
                                            The range is using half-open interval
                                            semantics [start, end).'
                                          properties:
                                            end:
                                              description: end of the range (exclusive)
                                              format: double
                                              type: number
                                            start:
                                              description: start of the range (inclusive)
                                              format: double
                                              type: number
                                          type: object
                                      type: object
                                    list_match:
                                      description: If specified, a match occurs if
                                        and only if the target value is a list value
                                        and  is matched to this field.
                                      properties:
                                        one_of:
                                          description: If specified, at least one
                                            of the values in the list must match the
                                            value specified.
                                          type: object
                                          x-kubernetes-preserve-unknown-fields: true
                                      type: object
                                    null_match:
                                      description: If specified, a match occurs if
                                        and only if the target value is a NullValue.
                                      type: object
                                      x-kubernetes-preserve-unknown-fields: true
                                    or_match:
                                      description: If specified, a match occurs if
                                        and only if any of the alternatives in the
                                        match accept the value.
                                      properties:
                                        value_matchers:
                                          items:
                                            type: object
                                            x-kubernetes-preserve-unknown-fields: true
                                          type: array
                                      type: object
                                    present_match:
                                      description: If specified, value match will
                                        be performed based on whether the path is
                                        referring to a  valid primitive value in the
                                        metadata. If the path is referring to a non-primitive
                                        value,  the result is always not matched.
                                      type: boolean
                                    string_match:
                                      description: If specified, a match occurs if
                                        and only if the target value is a string value
                                        and is  matched to this field.
                                      properties:
                                        contains:
                                          description: 'The input string must have
                                            the substring specified here.   .. note::    Empty
                                            contains match is not allowed, please
                                            use ``safe_regex`` instead.   Examples:   *
                                            ``abc`` matches the value ``xyz.abc.def``'
                                          type: string
                                        custom:
                                          description: 'Use an extension as the matcher
                                            type.  [#extension-category: envoy.string_matcher]'
                                          type: object
                                          x-kubernetes-preserve-unknown-fields: true
                                        exact:
                                          description: 'The input string must match
                                            exactly the string specified here.   Examples:   *
                                            ``abc`` only matches the value ``abc``.'
                                          type: string
                                        ignore_case:
                                          description: If ``true``, indicates the
                                            exact/prefix/suffix/contains matching
                                            should be case insensitive. This  has
                                            no effect for the ``safe_regex`` match.  For
                                            example, the matcher ``data`` will match
                                            both input string ``Data`` and ``data``
                                            if this option  is set to ``true``.
                                          type: boolean
                                        prefix:
                                          description: 'The input string must have
                                            the prefix specified here.   .. note::    Empty
                                            prefix match is not allowed, please use
                                            ``safe_regex`` instead.   Examples:   *
                                            ``abc`` matches the value ``abc.xyz``'
                                          type: string
                                        safe_regex:
                                          description: The input string must match
                                            the regular expression specified here.
                                          properties:
                                            google_re2:
                                              description: Google's RE2 regex engine.
                                              type: object
                                              x-kubernetes-preserve-unknown-fields: true
                                            regex:
                                              description: The regex match string.
                                                The string must be supported by the
                                                configured engine. The regex is matched  against
                                                the full string, not as a partial
                                                match.
                                              type: string
                                          type: object
                                        suffix:
                                          description: 'The input string must have
                                            the suffix specified here.   .. note::    Empty
                                            suffix match is not allowed, please use
                                            ``safe_regex`` instead.   Examples:   *
                                            ``abc`` matches the value ``xyz.abc``'
                                          type: string
                                      type: object
                                  type: object
                              type: object
                          type: object
                        not_health_check_filter:
                          description: Not health check filter.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        or_filter:
                          description: Or filter.
                          properties:
                            filters:
                              items:
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              type: array
                          type: object
                        response_flag_filter:
                          description: Response flag filter.
                          properties:
                            flags:
                              description: Only responses with the any of the flags
                                listed in this field will be  logged. This field is
                                optional. If it is not specified, then any response  flag
                                will pass the filter check.
                              items:
                                type: string
                              type: array
                          type: object
                        runtime_filter:
                          description: Runtime filter.
                          properties:
                            percent_sampled:
                              description: Defines the default sampling percentage
                                when no runtime override is present. If not specified,
                                the default is  **0%** (with a denominator of 100).
                              properties:
                                denominator:
                                  description: Specifies the denominator. If the denominator
                                    specified is less than the numerator, the final  fractional
                                    percentage is capped at 1 (100%).
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                numerator:
                                  description: Specifies the numerator. Defaults to
                                    0.
                                  format: int32
                                  type: integer
                              type: object
                            runtime_key:
                              description: Specifies a key used to look up a custom
                                sampling rate from the runtime configuration. If a
                                value is found for this  key, it will override the
                                default sampling rate specified in ``percent_sampled``.
                              type: string
                            use_independent_randomness:
                              description: 'Controls how sampling decisions are made.   -
                                Default behavior (``false``):     * Uses the :ref:`x-request-id<config_http_conn_man_headers_x-request-id>`
                                as a consistent sampling pivot.    * When :ref:`x-request-id<config_http_conn_man_headers_x-request-id>`
                                is present, sampling will be consistent      across
                                multiple hosts based on both the ``runtime_key`` and      :ref:`x-request-id<config_http_conn_man_headers_x-request-id>`.    *
                                Useful for tracking related requests across a distributed
                                system.   - When set to ``true`` or :ref:`x-request-id<config_http_conn_man_headers_x-request-id>`
                                is missing:     * Sampling decisions are made randomly
                                based only on the ``runtime_key``.    * Useful in
                                complex filter configurations (like nested      :ref:`AndFilter<envoy_v3_api_msg_config.accesslog.v3.AndFilter>`/      :ref:`OrFilter<envoy_v3_api_msg_config.accesslog.v3.OrFilter>`
                                blocks) where independent probability      calculations
                                are desired.    * Can be used to implement logging
                                kill switches with predictable probability distributions.'
                              type: boolean
                          type: object
                        status_code_filter:
                          description: Status code filter.
                          properties:
                            comparison:
                              description: Comparison.
                              properties:
                                op:
                                  description: Comparison operator.
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                value:
                                  description: Value to compare against.
                                  properties:
                                    default_value:
                                      description: Default value if runtime value
                                        is not available.
                                      format: int32
                                      type: integer
                                    runtime_key:
                                      description: Runtime key to get value for comparison.
                                        This value is used if defined.
                                      type: string
                                  type: object
                              type: object
                          type: object
                        traceable_filter:
                          description: Traceable filter.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    name:
                      description: The name of the access log extension configuration.
                      type: string
                    typed_config:
                      description: 'The type in this field determines the type of
                        API listener. At present, the following  types are supported:  envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                        (HTTP)  envoy.extensions.filters.network.http_connection_manager.v3.EnvoyMobileHttpConnectionManager
                        (HTTP)  [#next-major-version: In the v3 API, replace this
                        Any field with a oneof containing the  specific config message
                        for each type of API listener. We could not do this in v2
                        because  it would have caused circular dependencies for go
                        protos: lds.proto depends on this file,  and http_connection_manager.proto
                        depends on rds.proto, which is in the same directory as  lds.proto,
                        so lds.proto cannot depend on this file.]'
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
              additional_addresses:
                description: The additional addresses the listener should listen on.
                  The addresses must be unique across all  listeners. Multiple addresses
                  with port 0 can be supplied. When using multiple addresses in a
                  single listener,  all addresses use the same protocol, and multiple
                  internal addresses are not supported.
                items:
                  description: The additional address the listener is listening on.
                  properties:
                    address:
                      description: Addresses specify either a logical or physical
                        address and port, which are  used to tell Envoy where to bind/listen,
                        connect to upstream and find  management servers.
                      properties:
                        envoy_internal_address:
                          description: Specifies a user-space address handled by :ref:`internal
                            listeners  <envoy_v3_api_field_config.listener.v3.Listener.internal_listener>`.
                          properties:
                            endpoint_id:
                              description: Specifies an endpoint identifier to distinguish
                                between multiple endpoints for the same internal listener
                                in a  single upstream pool. Only used in the upstream
                                addresses for tracking changes to individual endpoints.
                                This, for  example, may be set to the final destination
                                IP for the target internal listener.
                              type: string
                            server_listener_name:
                              description: Specifies the :ref:`name <envoy_v3_api_field_config.listener.v3.Listener.name>`
                                of the  internal listener.
                              type: string
                          type: object
                        pipe:
                          properties:
                            mode:
                              description: The mode for the Pipe. Not applicable for
                                abstract sockets.
                              format: int32
                              type: integer
                            path:
                              description: Unix Domain Socket path. On Linux, paths
                                starting with '@' will use the  abstract namespace.
                                The starting '@' is replaced by a null byte by Envoy.  Paths
                                starting with '@' will result in an error in environments
                                other than  Linux.
                              type: string
                          type: object
                        socket_address:
                          description: '[#next-free-field: 8]'
                          properties:
                            address:
                              description: 'The address for this socket. :ref:`Listeners
                                <config_listeners>` will bind  to the address. An
                                empty address is not allowed. Specify ``0.0.0.0``
                                or ``::``  to bind to any address. [#comment:TODO(zuercher)
                                reinstate when implemented:  It is possible to distinguish
                                a Listener address via the prefix/suffix matching  in
                                :ref:`FilterChainMatch <envoy_v3_api_msg_config.listener.v3.FilterChainMatch>`.]
                                When used  within an upstream :ref:`BindConfig <envoy_v3_api_msg_config.core.v3.BindConfig>`,
                                the address  controls the source address of outbound
                                connections. For :ref:`clusters  <envoy_v3_api_msg_config.cluster.v3.Cluster>`,
                                the cluster type determines whether the  address must
                                be an IP (``STATIC`` or ``EDS`` clusters) or a hostname
                                resolved by DNS  (``STRICT_DNS`` or ``LOGICAL_DNS``
                                clusters). Address resolution can be customized  via
                                :ref:`resolver_name <envoy_v3_api_field_config.core.v3.SocketAddress.resolver_name>`.'
                              type: string
                            ipv4_compat:
                              description: When binding to an IPv6 address above,
                                this enables `IPv4 compatibility  <https://tools.ietf.org/html/rfc3493#page-11>`_.
                                Binding to ``::`` will  allow both IPv4 and IPv6 connections,
                                with peer IPv4 addresses mapped into  IPv6 space as
                                ``::FFFF:<IPv4-address>``.
                              type: boolean
                            named_port:
                              description: This is only valid if :ref:`resolver_name  <envoy_v3_api_field_config.core.v3.SocketAddress.resolver_name>`
                                is specified below and the  named resolver is capable
                                of named port resolution.
                              type: string
                            network_namespace_filepath:
                              description: 'Filepath that specifies the Linux network
                                namespace this socket will be created in (see ``man
                                7  network_namespaces``). If this field is set, Envoy
                                will create the socket in the specified  network namespace.   ..
                                note::     Setting this parameter requires Envoy to
                                run with the ``CAP_NET_ADMIN`` capability.   .. attention::      Network
                                namespaces are only configurable on Linux. Otherwise,
                                this field has no effect.'
                              type: string
                            port_value:
                              format: int32
                              type: integer
                            protocol:
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            resolver_name:
                              description: The name of the custom resolver. This must
                                have been registered with Envoy. If  this is empty,
                                a context dependent default applies. If the address
                                is a concrete  IP address, no resolution will occur.
                                If address is a hostname this  should be set for resolution
                                other than DNS. Specifying a custom resolver with  ``STRICT_DNS``
                                or ``LOGICAL_DNS`` will generate an error at runtime.
                              type: string
                          type: object
                      type: object
                    socket_options:
                      description: Additional socket options that may not be present
                        in Envoy source code or  precompiled binaries. If specified,
                        this will override the  :ref:`socket_options <envoy_v3_api_field_config.listener.v3.Listener.socket_options>`  in
                        the listener. If specified with no  :ref:`socket_options <envoy_v3_api_field_config.core.v3.SocketOptionsOverride.socket_options>`  or
                        an empty list of :ref:`socket_options <envoy_v3_api_field_config.core.v3.SocketOptionsOverride.socket_options>`,  it
                        means no socket option will apply.
                      properties:
                        socket_options:
                          items:
                            description: 'Generic socket option message. This would
                              be used to set socket options that  might not exist
                              in upstream kernels or precompiled Envoy binaries.   For
                              example:   .. code-block:: json    {     "description":
                              "support tcp keep alive",     "state": 0,     "level":
                              1,     "name": 9,     "int_value": 1,   }   1 means
                              SOL_SOCKET and 9 means SO_KEEPALIVE on Linux.  With
                              the above configuration, `TCP Keep-Alives <https://www.freesoft.org/CIE/RFC/1122/114.htm>`_  can
                              be enabled in socket with Linux, which can be used in  :ref:`listener''s<envoy_v3_api_field_config.listener.v3.Listener.socket_options>`
                              or  :ref:`admin''s <envoy_v3_api_field_config.bootstrap.v3.Admin.socket_options>`
                              socket_options etc.   It should be noted that the name
                              or level may have different values on different platforms.  [#next-free-field:
                              8]'
                            properties:
                              buf_value:
                                description: Otherwise it's a byte buffer.
                                format: byte
                                type: string
                              description:
                                description: An optional name to give this socket
                                  option for debugging, etc.  Uniqueness is not required
                                  and no special meaning is assumed.
                                type: string
                              int_value:
                                description: Because many sockopts take an int value.
                                format: int64
                                type: integer
                              level:
                                description: Corresponding to the level value passed
                                  to setsockopt, such as IPPROTO_TCP
                                format: int64
                                type: integer
                              name:
                                description: The numeric name as passed to setsockopt
                                format: int64
                                type: integer
                              state:
                                description: The state in which the option will be
                                  applied. When used in BindConfig  STATE_PREBIND
                                  is currently the only valid value.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              type:
                                description: Apply the socket option to the specified
                                  `socket type <https://linux.die.net/man/2/socket>`_.  If
                                  not specified, the socket option will be applied
                                  to all socket types.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                            type: object
                          type: array
                      type: object
                    tcp_keepalive:
                      description: Configures TCP keepalive settings for the additional
                        address.  If not set, the listener :ref:`tcp_keepalive <envoy_v3_api_field_config.listener.v3.Listener.tcp_keepalive>`  configuration
                        is inherited. You can explicitly disable TCP keepalive for
                        the additional address by setting any keepalive field  (:ref:`keepalive_probes
                        <envoy_v3_api_field_config.core.v3.TcpKeepalive.keepalive_probes>`,  :ref:`keepalive_time
                        <envoy_v3_api_field_config.core.v3.TcpKeepalive.keepalive_time>`,
                        or  :ref:`keepalive_interval <envoy_v3_api_field_config.core.v3.TcpKeepalive.keepalive_interval>`)
                        to ``0``.
                      properties:
                        keepalive_interval:
                          description: The number of seconds between keep-alive probes.
                            Default is to use the OS  level configuration (unless
                            overridden, Linux defaults to 75s.) Setting this to  ``0``
                            disables TCP keepalive.
                          format: int32
                          type: integer
                        keepalive_probes:
                          description: Maximum number of keepalive probes to send
                            without response before deciding  the connection is dead.
                            Default is to use the OS level configuration (unless  overridden,
                            Linux defaults to 9.) Setting this to ``0`` disables TCP
                            keepalive.
                          format: int32
                          type: integer
                        keepalive_time:
                          description: The number of seconds a connection needs to
                            be idle before keep-alive probes  start being sent. Default
                            is to use the OS level configuration (unless  overridden,
                            Linux defaults to 7200s (i.e., 2 hours.) Setting this
                            to ``0`` disables  TCP keepalive.
                          format: int32
                          type: integer
                      type: object
                  type: object
                type: array
              address:
                description: The address that the listener should listen on. In general,
                  the address must be unique, though  that is governed by the bind
                  rules of the OS. E.g., multiple listeners can listen on port 0 on  Linux
                  as the actual port will be allocated by the OS.  Required unless
                  ``api_listener`` or ``listener_specifier`` is populated.   When
                  the address contains a network namespace filepath (via  :ref:`network_namespace_filepath
                  <envoy_v3_api_field_config.core.v3.SocketAddress.network_namespace_filepath>`),  Envoy
                  automatically populates the filter state with key ``envoy.network.network_namespace``  when
                  a connection is accepted. This provides read-only access to the
                  network namespace for  filters, access logs, and other components.
                properties:
                  envoy_internal_address:
                    description: Specifies a user-space address handled by :ref:`internal
                      listeners  <envoy_v3_api_field_config.listener.v3.Listener.internal_listener>`.
                    properties:
                      endpoint_id:
                        description: Specifies an endpoint identifier to distinguish
                          between multiple endpoints for the same internal listener
                          in a  single upstream pool. Only used in the upstream addresses
                          for tracking changes to individual endpoints. This, for  example,
                          may be set to the final destination IP for the target internal
                          listener.
                        type: string
                      server_listener_name:
                        description: Specifies the :ref:`name <envoy_v3_api_field_config.listener.v3.Listener.name>`
                          of the  internal listener.
                        type: string
                    type: object
                  pipe:
                    properties:
                      mode:
                        description: The mode for the Pipe. Not applicable for abstract
                          sockets.
                        format: int32
                        type: integer
                      path:
                        description: Unix Domain Socket path. On Linux, paths starting
                          with '@' will use the  abstract namespace. The starting
                          '@' is replaced by a null byte by Envoy.  Paths starting
                          with '@' will result in an error in environments other than  Linux.
                        type: string
                    type: object
                  socket_address:
                    description: '[#next-free-field: 8]'
                    properties:
                      address:
                        description: 'The address for this socket. :ref:`Listeners
                          <config_listeners>` will bind  to the address. An empty
                          address is not allowed. Specify ``0.0.0.0`` or ``::``  to
                          bind to any address. [#comment:TODO(zuercher) reinstate
                          when implemented:  It is possible to distinguish a Listener
                          address via the prefix/suffix matching  in :ref:`FilterChainMatch
                          <envoy_v3_api_msg_config.listener.v3.FilterChainMatch>`.]
                          When used  within an upstream :ref:`BindConfig <envoy_v3_api_msg_config.core.v3.BindConfig>`,
                          the address  controls the source address of outbound connections.
                          For :ref:`clusters  <envoy_v3_api_msg_config.cluster.v3.Cluster>`,
                          the cluster type determines whether the  address must be
                          an IP (``STATIC`` or ``EDS`` clusters) or a hostname resolved
                          by DNS  (``STRICT_DNS`` or ``LOGICAL_DNS`` clusters). Address
                          resolution can be customized  via :ref:`resolver_name <envoy_v3_api_field_config.core.v3.SocketAddress.resolver_name>`.'
                        type: string
                      ipv4_compat:
                        description: When binding to an IPv6 address above, this enables
                          `IPv4 compatibility  <https://tools.ietf.org/html/rfc3493#page-11>`_.
                          Binding to ``::`` will  allow both IPv4 and IPv6 connections,
                          with peer IPv4 addresses mapped into  IPv6 space as ``::FFFF:<IPv4-address>``.
                        type: boolean
                      named_port:
                        description: This is only valid if :ref:`resolver_name  <envoy_v3_api_field_config.core.v3.SocketAddress.resolver_name>`
                          is specified below and the  named resolver is capable of
                          named port resolution.
                        type: string
                      network_namespace_filepath:
                        description: 'Filepath that specifies the Linux network namespace
                          this socket will be created in (see ``man 7  network_namespaces``).
                          If this field is set, Envoy will create the socket in the
                          specified  network namespace.   .. note::     Setting this
                          parameter requires Envoy to run with the ``CAP_NET_ADMIN``
                          capability.   .. attention::      Network namespaces are
                          only configurable on Linux. Otherwise, this field has no
                          effect.'
                        type: string
                      port_value:
                        format: int32
                        type: integer
                      protocol:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      resolver_name:
                        description: The name of the custom resolver. This must have
                          been registered with Envoy. If  this is empty, a context
                          dependent default applies. If the address is a concrete  IP
                          address, no resolution will occur. If address is a hostname
                          this  should be set for resolution other than DNS. Specifying
                          a custom resolver with  ``STRICT_DNS`` or ``LOGICAL_DNS``
                          will generate an error at runtime.
                        type: string
                    type: object
                type: object
              api_listener:
                description: 'Used to represent an API listener, which is used in
                  non-proxy clients. The type of API  exposed to the non-proxy application
                  depends on the type of API listener.  When this field is set, no
                  other field except for :ref:`name<envoy_v3_api_field_config.listener.v3.Listener.name>`  should
                  be set.   .. note::    Currently only one ApiListener can be installed;
                  and it can only be done via bootstrap config,   not LDS.   [#next-major-version:
                  In the v3 API, instead of this messy approach where the socket  listener
                  fields are directly in the top-level Listener message and the API
                  listener types  are in the ApiListener message, the socket listener
                  messages should be in their own message,  and the top-level Listener
                  should essentially be a oneof that selects between the  socket listener
                  and the various types of API listener. That way, a given Listener
                  message  can structurally only contain the fields of the relevant
                  type.]'
                properties:
                  api_listener:
                    description: 'The type in this field determines the type of API
                      listener. At present, the following  types are supported:  envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                      (HTTP)  envoy.extensions.filters.network.http_connection_manager.v3.EnvoyMobileHttpConnectionManager
                      (HTTP)  [#next-major-version: In the v3 API, replace this Any
                      field with a oneof containing the  specific config message for
                      each type of API listener. We could not do this in v2 because  it
                      would have caused circular dependencies for go protos: lds.proto
                      depends on this file,  and http_connection_manager.proto depends
                      on rds.proto, which is in the same directory as  lds.proto,
                      so lds.proto cannot depend on this file.]'
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              bind_to_port:
                description: Whether the listener should bind to the port. A listener
                  that doesn't  bind can only receive connections redirected from
                  other listeners that set  :ref:`use_original_dst <envoy_v3_api_field_config.listener.v3.Listener.use_original_dst>`  to
                  true. Default is true.
                type: boolean
              bypass_overload_manager:
                description: Whether the listener bypasses configured overload manager
                  actions.
                type: boolean
              connection_balance_config:
                description: The listener's connection balancer configuration, currently
                  only applicable to TCP listeners.  If no configuration is specified,
                  Envoy will not attempt to balance active connections between  worker
                  threads.   In the scenario that the listener X redirects all the
                  connections to the listeners Y1 and Y2  by setting :ref:`use_original_dst
                  <envoy_v3_api_field_config.listener.v3.Listener.use_original_dst>`
                  in X  and :ref:`bind_to_port <envoy_v3_api_field_config.listener.v3.Listener.bind_to_port>`
                  to false in Y1 and Y2,  it is recommended to disable the balance
                  config in listener X to avoid the cost of balancing, and  enable
                  the balance config in Y1 and Y2 to balance the connections among
                  the workers.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              continue_on_listener_filters_timeout:
                description: 'Whether a connection should be created when listener
                  filters timeout. Default is false.   .. attention::     Some listener
                  filters, such as :ref:`Proxy Protocol filter    <config_listener_filters_proxy_protocol>`,
                  should not be used with this option. It will cause    unexpected
                  behavior when a connection is created.'
                type: boolean
              default_filter_chain:
                description: The default filter chain if none of the filter chain
                  matches. If no default filter chain is supplied,  the connection
                  will be closed. The filter chain match is ignored in this field.
                properties:
                  filter_chain_match:
                    description: The criteria to use when matching a connection to
                      this filter chain.
                    properties:
                      address_suffix:
                        description: 'If non-empty, an IP address and suffix length
                          to match addresses when the  listener is bound to 0.0.0.0/::
                          or when use_original_dst is specified.  [#not-implemented-hide:]'
                        type: string
                      application_protocols:
                        description: 'If non-empty, a list of application protocols
                          (e.g. ALPN for TLS protocol) to consider when  determining
                          a filter chain match. Those values will be compared against
                          the application  protocols of a new connection, when detected
                          by one of the listener filters.   Suggested values include:   *
                          ``http/1.1`` - set by :ref:`envoy.filters.listener.tls_inspector    <config_listener_filters_tls_inspector>`,  *
                          ``h2`` - set by :ref:`envoy.filters.listener.tls_inspector
                          <config_listener_filters_tls_inspector>`   .. attention::     Currently,
                          only :ref:`TLS Inspector <config_listener_filters_tls_inspector>`
                          provides    application protocol detection based on the
                          requested    `ALPN <https://en.wikipedia.org/wiki/Application-Layer_Protocol_Negotiation>`_
                          values.     However, the use of ALPN is pretty much limited
                          to the HTTP/2 traffic on the Internet,    and matching on
                          values other than ``h2`` is going to lead to a lot of false
                          negatives,    unless all connecting clients are known to
                          use ALPN.'
                        items:
                          type: string
                        type: array
                      destination_port:
                        description: Optional destination port to consider when use_original_dst
                          is set on the  listener in determining a filter chain match.
                        format: int32
                        type: integer
                      direct_source_prefix_ranges:
                        description: The criteria is satisfied if the directly connected
                          source IP address of the downstream  connection is contained
                          in at least one of the specified subnets. If the parameter
                          is not  specified or the list is empty, the directly connected
                          source IP address is ignored.
                        items:
                          description: CidrRange specifies an IP Address and a prefix
                            length to construct  the subnet mask for a `CIDR <https://tools.ietf.org/html/rfc4632>`_
                            range.
                          properties:
                            address_prefix:
                              description: IPv4 or IPv6 address, e.g. ``192.0.0.0``
                                or ``2001:db8::``.
                              type: string
                            prefix_len:
                              description: Length of prefix, e.g. 0, 32. Defaults
                                to 0 when unset.
                              format: int32
                              type: integer
                          type: object
                        type: array
                      prefix_ranges:
                        description: 'If non-empty, an IP address and prefix length
                          to match addresses when the  listener is bound to 0.0.0.0/::
                          or when use_original_dst is specified.'
                        items:
                          description: CidrRange specifies an IP Address and a prefix
                            length to construct  the subnet mask for a `CIDR <https://tools.ietf.org/html/rfc4632>`_
                            range.
                          properties:
                            address_prefix:
                              description: IPv4 or IPv6 address, e.g. ``192.0.0.0``
                                or ``2001:db8::``.
                              type: string
                            prefix_len:
                              description: Length of prefix, e.g. 0, 32. Defaults
                                to 0 when unset.
                              format: int32
                              type: integer
                          type: object
                        type: array
                      server_names:
                        description: 'If non-empty, a list of server names (e.g. SNI
                          for TLS protocol) to consider when determining  a filter
                          chain match. Those values will be compared against the server
                          names of a new  connection, when detected by one of the
                          listener filters.   The server name will be matched against
                          all wildcard domains, i.e. ``www.example.com``  will be
                          first matched against ``www.example.com``, then ``*.example.com``,
                          then ``*.com``.   Note that partial wildcards are not supported,
                          and values like ``*w.example.com`` are invalid.  The value
                          ``*`` is also not supported, and ``server_names`` should
                          be omitted instead.   .. attention::     See the :ref:`FAQ
                          entry <faq_how_to_setup_sni>` on how to configure SNI for
                          more    information.'
                        items:
                          type: string
                        type: array
                      source_ports:
                        description: The criteria is satisfied if the source port
                          of the downstream connection  is contained in at least one
                          of the specified ports. If the parameter is  not specified,
                          the source port is ignored.
                        items:
                          format: int32
                          type: integer
                        type: array
                      source_prefix_ranges:
                        description: The criteria is satisfied if the source IP address
                          of the downstream  connection is contained in at least one
                          of the specified subnets. If the  parameter is not specified
                          or the list is empty, the source IP address is  ignored.
                        items:
                          description: CidrRange specifies an IP Address and a prefix
                            length to construct  the subnet mask for a `CIDR <https://tools.ietf.org/html/rfc4632>`_
                            range.
                          properties:
                            address_prefix:
                              description: IPv4 or IPv6 address, e.g. ``192.0.0.0``
                                or ``2001:db8::``.
                              type: string
                            prefix_len:
                              description: Length of prefix, e.g. 0, 32. Defaults
                                to 0 when unset.
                              format: int32
                              type: integer
                          type: object
                        type: array
                      source_type:
                        description: Specifies the connection source IP match type.
                          Can be any, local or external network.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      suffix_len:
                        description: '[#not-implemented-hide:]'
                        format: int32
                        type: integer
                      transport_protocol:
                        description: 'If non-empty, a transport protocol to consider
                          when determining a filter chain match.  This value will
                          be compared against the transport protocol of a new connection,
                          when  it''s detected by one of the listener filters.   Suggested
                          values include:   * ``raw_buffer`` - default, used when
                          no transport protocol is detected,  * ``tls`` - set by :ref:`envoy.filters.listener.tls_inspector
                          <config_listener_filters_tls_inspector>`    when TLS protocol
                          is detected.'
                        type: string
                    type: object
                  filters:
                    description: 'A list of individual network filters that make up
                      the filter chain for  connections established with the listener.
                      Order matters as the filters are  processed sequentially as
                      connection events happen. Note: If the filter  list is empty,
                      the connection will close by default.   For QUIC listeners,
                      network filters other than HTTP Connection Manager (HCM)  can
                      be created, but due to differences in the connection implementation
                      compared  to TCP, the onData() method will never be called.
                      Therefore, network filters  for QUIC listeners should only expect
                      to do work at the start of a new connection  (i.e. in onNewConnection()).
                      HCM must be the last (or only) filter in the chain.'
                    items:
                      description: '[#next-free-field: 6]'
                      properties:
                        config_discovery:
                          description: Configuration source specifier for an extension
                            configuration discovery  service. In case of a failure
                            and without the default configuration, the  listener closes
                            the connections.
                          properties:
                            apply_default_config_without_warming:
                              description: Use the default config as the initial configuration
                                without warming and  waiting for the first discovery
                                response. Requires the default configuration  to be
                                supplied.
                              type: boolean
                            config_source:
                              description: 'Configuration for :ref:`listeners <config_listeners>`,
                                :ref:`clusters  <config_cluster_manager>`, :ref:`routes  <envoy_v3_api_msg_config.route.v3.RouteConfiguration>`,
                                :ref:`endpoints  <arch_overview_service_discovery>`
                                etc. may either be sourced from the  filesystem or
                                from an xDS API source. Filesystem configs are watched
                                with  inotify for updates.  [#next-free-field: 9]'
                              properties:
                                ads:
                                  description: When set, ADS will be used to fetch
                                    resources. The ADS API configuration  source in
                                    the bootstrap configuration is used.
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                api_config_source:
                                  description: API configuration source.
                                  properties:
                                    api_type:
                                      description: API type (gRPC, REST, delta gRPC)
                                      type: object
                                      x-kubernetes-preserve-unknown-fields: true
                                    cluster_names:
                                      description: 'Cluster names should be used only
                                        with REST. If > 1  cluster is defined, clusters
                                        will be cycled through if any kind of failure  occurs.   ..
                                        note::    The cluster with name ``cluster_name``
                                        must be statically defined and its   type
                                        must not be ``EDS``.'
                                      items:
                                        type: string
                                      type: array
                                    config_validators:
                                      description: 'A list of config validators that
                                        will be executed when a new update is  received
                                        from the ApiConfigSource. Note that each validator
                                        handles a  specific xDS service type, and
                                        only the validators corresponding to the  type
                                        url (in ``:ref: DiscoveryResponse`` or ``:ref:
                                        DeltaDiscoveryResponse``)  will be invoked.  If
                                        the validator returns false or throws an exception,
                                        the config will be rejected by  the client,
                                        and a NACK will be sent.  [#extension-category:
                                        envoy.config.validators]'
                                      items:
                                        description: 'Message type for extension configuration.  [#next-major-version:
                                          revisit all existing typed_config that doesn''t
                                          use this wrapper.].'
                                        properties:
                                          name:
                                            description: The name of an extension.
                                              This is not used to select the extension,
                                              instead  it serves the role of an opaque
                                              identifier.
                                            type: string
                                          typed_config:
                                            description: The typed config for the
                                              extension. The type URL will be used
                                              to identify  the extension. In the case
                                              that the type URL is ``xds.type.v3.TypedStruct``  (or,
                                              for historical reasons, ``udpa.type.v1.TypedStruct``),
                                              the inner type  URL of ``TypedStruct``
                                              will be utilized. See the  :ref:`extension
                                              configuration overview  <config_overview_extension_configuration>`
                                              for further details.
                                            type: object
                                            x-kubernetes-preserve-unknown-fields: true
                                        type: object
                                      type: array
                                    grpc_services:
                                      description: Multiple gRPC services be provided
                                        for GRPC. If > 1 cluster is defined,  services
                                        will be cycled through if any kind of failure
                                        occurs.
                                      items:
                                        description: 'gRPC service configuration.
                                          This is used by :ref:`ApiConfigSource  <envoy_v3_api_msg_config.core.v3.ApiConfigSource>`
                                          and filter configurations.  [#next-free-field:
                                          7]'
                                        properties:
                                          envoy_grpc:
                                            description: Envoy's in-built gRPC client.  See
                                              the :ref:`gRPC services overview <arch_overview_grpc_services>`  documentation
                                              for discussion on gRPC client selection.
                                            type: object
                                            x-kubernetes-preserve-unknown-fields: true
                                          google_grpc:
                                            description: '`Google C++ gRPC client
                                              <https://github.com/grpc/grpc>`_  See
                                              the :ref:`gRPC services overview <arch_overview_grpc_services>`  documentation
                                              for discussion on gRPC client selection.'
                                            type: object
                                            x-kubernetes-preserve-unknown-fields: true
                                          initial_metadata:
                                            description: 'Additional metadata to include
                                              in streams initiated to the GrpcService.
                                              This can be used for  scenarios in which
                                              additional ad hoc authorization headers
                                              (e.g. ``x-foo-bar: baz-key``) are to  be
                                              injected. For more information, including
                                              details on header value syntax, see
                                              the  documentation on :ref:`custom request
                                              headers  <config_http_conn_man_headers_custom_request_headers>`.'
                                            items:
                                              description: Header name/value pair.
                                              properties:
                                                key:
                                                  description: Header name.
                                                  type: string
                                                raw_value:
                                                  description: Header value is encoded
                                                    as bytes which can support non-utf8
                                                    characters.  Only one of ``value``
                                                    or ``raw_value`` can be set.
                                                  format: byte
                                                  type: string
                                                value:
                                                  description: Header value.   The
                                                    same :ref:`format specifier <config_access_log_format>`
                                                    as used for  :ref:`HTTP access
                                                    logging <config_access_log>` applies
                                                    here, however  unknown header
                                                    values are replaced with the empty
                                                    string instead of ``-``.  Header
                                                    value is encoded as string. This
                                                    does not work for non-utf8 characters.  Only
                                                    one of ``value`` or ``raw_value``
                                                    can be set.
                                                  type: string
                                              type: object
                                            type: array
                                          retry_policy:
                                            description: 'Optional default retry policy
                                              for RPCs or streams initiated toward
                                              this gRPC service.   If an async stream
                                              does not have a retry policy configured
                                              in its per‑stream options, this  policy
                                              is used as the default.   .. note::     This
                                              field is only applied by Envoy gRPC
                                              (``envoy_grpc``) clients. Google gRPC    (``google_grpc``)
                                              clients currently ignore this field.   If
                                              not specified, no default retry policy
                                              is applied at the client level and retries
                                              only occur  when explicitly configured
                                              in per‑stream options.'
                                            properties:
                                              host_selection_retry_max_attempts:
                                                description: For details, see :ref:`host_selection_retry_max_attempts
                                                  <envoy_v3_api_field_config.route.v3.RetryPolicy.host_selection_retry_max_attempts>`.
                                                format: int64
                                                type: integer
                                              num_retries:
                                                description: Specifies the allowed
                                                  number of retries. This parameter
                                                  is optional and  defaults to 1.
                                                format: int32
                                                type: integer
                                              retry_back_off:
                                                description: Specifies parameters
                                                  that control :ref:`retry backoff
                                                  strategy <envoy_v3_api_msg_config.core.v3.BackoffStrategy>`.  This
                                                  parameter is optional, in which
                                                  case the default base interval is
                                                  1000 milliseconds. The  default
                                                  maximum interval is 10 times the
                                                  base interval.
                                                properties:
                                                  base_interval:
                                                    description: The base interval
                                                      to be used for the next back
                                                      off computation. It should  be
                                                      greater than zero and less than
                                                      or equal to :ref:`max_interval  <envoy_v3_api_field_config.core.v3.BackoffStrategy.max_interval>`.
                                                    type: string
                                                  max_interval:
                                                    description: Specifies the maximum
                                                      interval between retries. This
                                                      parameter is optional,  but
                                                      must be greater than or equal
                                                      to the :ref:`base_interval  <envoy_v3_api_field_config.core.v3.BackoffStrategy.base_interval>`
                                                      if set. The default  is 10 times
                                                      the :ref:`base_interval  <envoy_v3_api_field_config.core.v3.BackoffStrategy.base_interval>`.
                                                    type: string
                                                type: object
                                              retry_host_predicate:
                                                description: For details, see :ref:`RetryHostPredicate
                                                  <envoy_v3_api_field_config.route.v3.RetryPolicy.retry_host_predicate>`.
                                                items:
                                                  type: object
                                                  x-kubernetes-preserve-unknown-fields: true
                                                type: array
                                              retry_on:
                                                description: For details, see :ref:`retry_on
                                                  <envoy_v3_api_field_config.route.v3.RetryPolicy.retry_on>`.
                                                type: string
                                              retry_priority:
                                                description: For details, see :ref:`retry_priority
                                                  <envoy_v3_api_field_config.route.v3.RetryPolicy.retry_priority>`.
                                                type: object
                                                x-kubernetes-preserve-unknown-fields: true
                                            type: object
                                          timeout:
                                            description: The timeout for the gRPC
                                              request. This is the timeout for a specific  request.
                                            type: string
                                        type: object
                                      type: array
                                    rate_limit_settings:
                                      description: For GRPC APIs, the rate limit settings.
                                        If present, discovery requests made by Envoy
                                        will be  rate limited.
                                      properties:
                                        fill_rate:
                                          description: Rate at which tokens will be
                                            filled per second. If not set, a default
                                            fill rate of 10 tokens  per second will
                                            be used. The minimal fill rate is once
                                            per year. Lower  fill rates will be set
                                            to once per year.
                                          format: double
                                          type: number
                                        max_tokens:
                                          description: Maximum number of tokens to
                                            be used for rate limiting discovery request
                                            calls. If not set, a  default value of
                                            100 will be used.
                                          format: int32
                                          type: integer
                                      type: object
                                    refresh_delay:
                                      description: For REST APIs, the delay between
                                        successive polls.
                                      type: string
                                    request_timeout:
                                      description: For REST APIs, the request timeout.
                                        If not set, a default value of 1s will be
                                        used.
                                      type: string
                                    set_node_on_first_message_only:
                                      description: Skip the node identifier in subsequent
                                        discovery requests for streaming gRPC config
                                        types.
                                      type: boolean
                                    transport_api_version:
                                      description: API version for xDS transport protocol.
                                        This describes the xDS gRPC/REST  endpoint
                                        and version of [Delta]DiscoveryRequest/Response
                                        used on the wire.
                                      enum:
                                      - AUTO
                                      - V2
                                      - V3
                                      type: string
                                  type: object
                                authorities:
                                  description: Authorities that this config source
                                    may be used for. An authority specified in a xdstp://
                                    URL  is resolved to a ``ConfigSource`` prior to
                                    configuration fetch. This field provides the  association
                                    between authority name and configuration source.  [#not-implemented-hide:]
                                  items:
                                    type: object
                                    x-kubernetes-preserve-unknown-fields: true
                                  type: array
                                initial_fetch_timeout:
                                  description: When this timeout is specified, Envoy
                                    will wait no longer than the specified time for
                                    first  config response on this xDS subscription
                                    during the :ref:`initialization process  <arch_overview_initialization>`.
                                    After reaching the timeout, Envoy will move to
                                    the next  initialization phase, even if the first
                                    config is not delivered yet. The timer is activated  when
                                    the xDS API subscription starts, and is disarmed
                                    on first config update or on error. 0  means no
                                    timeout - Envoy will wait indefinitely for the
                                    first xDS config (unless another  timeout applies).
                                    The default is 15s.
                                  type: string
                                path:
                                  description: Deprecated in favor of ``path_config_source``.
                                    Use that field instead.
                                  type: string
                                path_config_source:
                                  description: Local filesystem path configuration
                                    source.
                                  properties:
                                    path:
                                      description: 'Path on the filesystem to source
                                        and watch for configuration updates.  When
                                        sourcing configuration for a :ref:`secret
                                        <envoy_v3_api_msg_extensions.transport_sockets.tls.v3.Secret>`,  the
                                        certificate and key files are also watched
                                        for updates.   .. note::    The path to the
                                        source must exist at config load time.   ..
                                        note::     If ``watched_directory`` is *not*
                                        configured, Envoy will watch the file path
                                        for *moves*.    This is because in general
                                        only moves are atomic. The same method of
                                        swapping files as is    demonstrated in the
                                        :ref:`runtime documentation <config_runtime_symbolic_link_swap>`
                                        can be    used here also. If ``watched_directory``
                                        is configured, no watch will be placed directly
                                        on    this path. Instead, the configured ``watched_directory``
                                        will be used to trigger reloads of    this
                                        path. This is required in certain deployment
                                        scenarios. See below for more information.'
                                      type: string
                                    watched_directory:
                                      description: 'If configured, this directory
                                        will be watched for *moves*. When an entry
                                        in this directory is  moved to, the ``path``
                                        will be reloaded. This is required in certain
                                        deployment scenarios.   Specifically, if trying
                                        to load an xDS resource using a  `Kubernetes
                                        ConfigMap <https://kubernetes.io/docs/concepts/configuration/configmap/>`_,
                                        the  following configuration might be used:  1.
                                        Store xds.yaml inside a ConfigMap.  2. Mount
                                        the ConfigMap to ``/config_map/xds``  3. Configure
                                        path ``/config_map/xds/xds.yaml``  4. Configure
                                        watched directory ``/config_map/xds``   The
                                        above configuration will ensure that Envoy
                                        watches the owning directory for moves which
                                        is  required due to how Kubernetes manages
                                        ConfigMap symbolic links during atomic updates.'
                                      properties:
                                        path:
                                          description: Directory path to watch.
                                          type: string
                                      type: object
                                  type: object
                                resource_api_version:
                                  description: API version for xDS resources. This
                                    implies the type URLs that the client  will request
                                    for resources and the resource type that the client
                                    will in  turn expect to be delivered.
                                  enum:
                                  - AUTO
                                  - V2
                                  - V3
                                  type: string
                                self:
                                  description: '[#not-implemented-hide:]  When set,
                                    the client will access the resources from the
                                    same server it got the  ConfigSource from, although
                                    not necessarily from the same stream. This is
                                    similar to the  :ref:`ads<envoy_v3_api_field.ConfigSource.ads>`
                                    field, except that the client may use a  different
                                    stream to the same server. As a result, this field
                                    can be used for things  like LRS that cannot be
                                    sent on an ADS stream. It can also be used to
                                    link from (e.g.)  LDS to RDS on the same server
                                    without requiring the management server to know
                                    its name  or required credentials.  [#next-major-version:
                                    In xDS v3, consider replacing the ads field with
                                    this one, since  this field can implicitly mean
                                    to use the same stream in the case where the ConfigSource  is
                                    provided via ADS and the specified data can also
                                    be obtained via ADS.]'
                                  properties:
                                    transport_api_version:
                                      description: API version for xDS transport protocol.
                                        This describes the xDS gRPC/REST  endpoint
                                        and version of [Delta]DiscoveryRequest/Response
                                        used on the wire.
                                      enum:
                                      - AUTO
                                      - V2
                                      - V3
                                      type: string
                                  type: object
                              type: object
                            default_config:
                              description: Optional default configuration to use as
                                the initial configuration if  there is a failure to
                                receive the initial extension configuration or if  ``apply_default_config_without_warming``
                                flag is set.
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            type_urls:
                              description: A set of permitted extension type URLs
                                for the type encoded inside of the  :ref:`TypedExtensionConfig
                                <envoy_v3_api_msg_config.core.v3.TypedExtensionConfig>`.
                                Extension  configuration updates are rejected if they
                                do not match any type URL in the set.
                              items:
                                type: string
                              type: array
                          type: object
                        name:
                          description: The name of the filter configuration.
                          type: string
                        typed_config:
                          description: 'Filter specific configuration which depends
                            on the filter being  instantiated. See the supported filters
                            for further documentation.  [#extension-category: envoy.filters.network]'
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    type: array
                  metadata:
                    description: Filter chain metadata.
                    properties:
                      filter_metadata:
                        additionalProperties:
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        type: object
                      typed_filter_metadata:
                        additionalProperties:
                          description: 'The type in this field determines the type
                            of API listener. At present, the following  types are
                            supported:  envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                            (HTTP)  envoy.extensions.filters.network.http_connection_manager.v3.EnvoyMobileHttpConnectionManager
                            (HTTP)  [#next-major-version: In the v3 API, replace this
                            Any field with a oneof containing the  specific config
                            message for each type of API listener. We could not do
                            this in v2 because  it would have caused circular dependencies
                            for go protos: lds.proto depends on this file,  and http_connection_manager.proto
                            depends on rds.proto, which is in the same directory as  lds.proto,
                            so lds.proto cannot depend on this file.]'
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        type: object
                    type: object
                  name:
                    description: 'The unique name (or empty) by which this filter
                      chain is known.   .. note::      :ref:`filter_chain_matcher      <envoy_v3_api_field_config.listener.v3.Listener.filter_chain_matcher>`      requires
                      that filter chains are uniquely named within a listener.'
                    type: string
                  transport_socket:
                    description: 'Optional custom transport socket implementation
                      to use for downstream connections.  To setup TLS, set a transport
                      socket with name ``envoy.transport_sockets.tls`` and  :ref:`DownstreamTlsContext
                      <envoy_v3_api_msg_extensions.transport_sockets.tls.v3.DownstreamTlsContext>`
                      in the ``typed_config``.  If no transport socket configuration
                      is specified, new connections  will be set up with plaintext.  [#extension-category:
                      envoy.transport_sockets.downstream]'
                    properties:
                      name:
                        description: The name of the transport socket to instantiate.
                          The name must match a supported transport  socket implementation.
                        type: string
                      typed_config:
                        description: Optional default configuration to use as the
                          initial configuration if  there is a failure to receive
                          the initial extension configuration or if  ``apply_default_config_without_warming``
                          flag is set.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                  transport_socket_connect_timeout:
                    description: If present and nonzero, the amount of time to allow
                      incoming connections to complete any  transport socket negotiations.
                      If this expires before the transport reports connection  establishment,
                      the connection is summarily closed.
                    type: string
                  use_proxy_proto:
                    description: Whether the listener should expect a PROXY protocol
                      V1 header on new  connections. If this option is enabled, the
                      listener will assume that that  remote address of the connection
                      is the one specified in the header. Some  load balancers including
                      the AWS ELB support this option. If the option is  absent or
                      set to false, Envoy will use the physical peer address of the  connection
                      as the remote address.   This field is deprecated. Add a  :ref:`PROXY
                      protocol listener filter <config_listener_filters_proxy_protocol>`  explicitly
                      instead.
                    type: boolean
                type: object
              deprecated_v1:
                description: '[#not-implemented-hide:]'
                type: object
                x-kubernetes-preserve-unknown-fields: true
              drain_type:
                description: The type of draining to perform at a listener-wide level.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              enable_mptcp:
                description: Enable MPTCP (multi-path TCP) on this listener. Clients
                  will be allowed to establish  MPTCP connections. Non-MPTCP clients
                  will fall back to regular TCP.
                type: boolean
              enable_reuse_port:
                description: 'When this flag is set to true, listeners set the ``SO_REUSEPORT``
                  socket option and  create one socket for each worker thread. This
                  makes inbound connections  distribute among worker threads roughly
                  evenly in cases where there are a high number  of connections. When
                  this flag is set to false, all worker threads share one socket.
                  This field  defaults to true. The change of field will be rejected
                  during an listener update when the  runtime flag ``envoy.reloadable_features.enable_update_listener_socket_options``
                  is enabled.  Otherwise, the update of this field will be ignored
                  quietly.   .. attention::     Although this field defaults to true,
                  it has different behavior on different platforms. See    the following
                  text for more information.   * On Linux, reuse_port is respected
                  for both TCP and UDP listeners. It also works correctly    with
                  hot restart.  * On macOS, reuse_port for TCP does not do what it
                  does on Linux. Instead of load balancing,    the last socket wins
                  and receives all connections/packets. For TCP, reuse_port is force    disabled
                  and the user is warned. For UDP, it is enabled, but only one worker
                  will receive    packets. For QUIC/H3, SW routing will send packets
                  to other workers. For "raw" UDP, only    a single worker will currently
                  receive packets.  * On Windows, reuse_port for TCP has undefined
                  behavior. It is force disabled and the user    is warned similar
                  to macOS. It is left enabled for UDP with undefined behavior currently.'
                type: boolean
              fcds_config:
                description: Discover filter chains configurations by external service.
                  Dynamic discovery of filter chains is allowed  while having statically
                  configured filter chains, however, a filter chain name must be unique
                  within a  listener. If a discovered filter chain matches a name
                  of an existing filter chain, it is discarded.  [#not-implemented-hide:]
                type: object
                x-kubernetes-preserve-unknown-fields: true
              filter_chain_matcher:
                description: ':ref:`Matcher API <arch_overview_matching_listener>`
                  resolving the filter chain name from the  network properties. This
                  matcher is used as a replacement for the filter chain match condition  :ref:`filter_chain_match  <envoy_v3_api_field_config.listener.v3.FilterChain.filter_chain_match>`.
                  If specified, all  :ref:`filter_chains <envoy_v3_api_field_config.listener.v3.Listener.filter_chains>`
                  must have a  non-empty and unique :ref:`name <envoy_v3_api_field_config.listener.v3.FilterChain.name>`
                  field  and not specify :ref:`filter_chain_match  <envoy_v3_api_field_config.listener.v3.FilterChain.filter_chain_match>`
                  field.   .. note::    Once matched, each connection is permanently
                  bound to its filter chain.   If the matcher changes but the filter
                  chain remains the same, the   connections bound to the filter chain
                  are not drained. If, however, the   filter chain is removed or structurally
                  modified, then the drain for its   connections is initiated.'
                type: object
                x-kubernetes-preserve-unknown-fields: true
              filter_chains:
                description: A list of filter chains to consider for this listener.
                  The  :ref:`FilterChain <envoy_v3_api_msg_config.listener.v3.FilterChain>`
                  with the most specific  :ref:`FilterChainMatch <envoy_v3_api_msg_config.listener.v3.FilterChainMatch>`
                  criteria is used on a  connection.   Example using SNI for filter
                  chain selection can be found in the  :ref:`FAQ entry <faq_how_to_setup_sni>`.
                items:
                  description: 'A filter chain wraps a set of match criteria, an option
                    TLS context, a set of filters, and  various other parameters.  [#next-free-field:
                    10]'
                  properties:
                    filter_chain_match:
                      description: The criteria to use when matching a connection
                        to this filter chain.
                      properties:
                        address_suffix:
                          description: 'If non-empty, an IP address and suffix length
                            to match addresses when the  listener is bound to 0.0.0.0/::
                            or when use_original_dst is specified.  [#not-implemented-hide:]'
                          type: string
                        application_protocols:
                          description: 'If non-empty, a list of application protocols
                            (e.g. ALPN for TLS protocol) to consider when  determining
                            a filter chain match. Those values will be compared against
                            the application  protocols of a new connection, when detected
                            by one of the listener filters.   Suggested values include:   *
                            ``http/1.1`` - set by :ref:`envoy.filters.listener.tls_inspector    <config_listener_filters_tls_inspector>`,  *
                            ``h2`` - set by :ref:`envoy.filters.listener.tls_inspector
                            <config_listener_filters_tls_inspector>`   .. attention::     Currently,
                            only :ref:`TLS Inspector <config_listener_filters_tls_inspector>`
                            provides    application protocol detection based on the
                            requested    `ALPN <https://en.wikipedia.org/wiki/Application-Layer_Protocol_Negotiation>`_
                            values.     However, the use of ALPN is pretty much limited
                            to the HTTP/2 traffic on the Internet,    and matching
                            on values other than ``h2`` is going to lead to a lot
                            of false negatives,    unless all connecting clients are
                            known to use ALPN.'
                          items:
                            type: string
                          type: array
                        destination_port:
                          description: Optional destination port to consider when
                            use_original_dst is set on the  listener in determining
                            a filter chain match.
                          format: int32
                          type: integer
                        direct_source_prefix_ranges:
                          description: The criteria is satisfied if the directly connected
                            source IP address of the downstream  connection is contained
                            in at least one of the specified subnets. If the parameter
                            is not  specified or the list is empty, the directly connected
                            source IP address is ignored.
                          items:
                            description: CidrRange specifies an IP Address and a prefix
                              length to construct  the subnet mask for a `CIDR <https://tools.ietf.org/html/rfc4632>`_
                              range.
                            properties:
                              address_prefix:
                                description: IPv4 or IPv6 address, e.g. ``192.0.0.0``
                                  or ``2001:db8::``.
                                type: string
                              prefix_len:
                                description: Length of prefix, e.g. 0, 32. Defaults
                                  to 0 when unset.
                                format: int32
                                type: integer
                            type: object
                          type: array
                        prefix_ranges:
                          description: 'If non-empty, an IP address and prefix length
                            to match addresses when the  listener is bound to 0.0.0.0/::
                            or when use_original_dst is specified.'
                          items:
                            description: CidrRange specifies an IP Address and a prefix
                              length to construct  the subnet mask for a `CIDR <https://tools.ietf.org/html/rfc4632>`_
                              range.
                            properties:
                              address_prefix:
                                description: IPv4 or IPv6 address, e.g. ``192.0.0.0``
                                  or ``2001:db8::``.
                                type: string
                              prefix_len:
                                description: Length of prefix, e.g. 0, 32. Defaults
                                  to 0 when unset.
                                format: int32
                                type: integer
                            type: object
                          type: array
                        server_names:
                          description: 'If non-empty, a list of server names (e.g.
                            SNI for TLS protocol) to consider when determining  a
                            filter chain match. Those values will be compared against
                            the server names of a new  connection, when detected by
                            one of the listener filters.   The server name will be
                            matched against all wildcard domains, i.e. ``www.example.com``  will
                            be first matched against ``www.example.com``, then ``*.example.com``,
                            then ``*.com``.   Note that partial wildcards are not
                            supported, and values like ``*w.example.com`` are invalid.  The
                            value ``*`` is also not supported, and ``server_names``
                            should be omitted instead.   .. attention::     See the
                            :ref:`FAQ entry <faq_how_to_setup_sni>` on how to configure
                            SNI for more    information.'
                          items:
                            type: string
                          type: array
                        source_ports:
                          description: The criteria is satisfied if the source port
                            of the downstream connection  is contained in at least
                            one of the specified ports. If the parameter is  not specified,
                            the source port is ignored.
                          items:
                            format: int32
                            type: integer
                          type: array
                        source_prefix_ranges:
                          description: The criteria is satisfied if the source IP
                            address of the downstream  connection is contained in
                            at least one of the specified subnets. If the  parameter
                            is not specified or the list is empty, the source IP address
                            is  ignored.
                          items:
                            description: CidrRange specifies an IP Address and a prefix
                              length to construct  the subnet mask for a `CIDR <https://tools.ietf.org/html/rfc4632>`_
                              range.
                            properties:
                              address_prefix:
                                description: IPv4 or IPv6 address, e.g. ``192.0.0.0``
                                  or ``2001:db8::``.
                                type: string
                              prefix_len:
                                description: Length of prefix, e.g. 0, 32. Defaults
                                  to 0 when unset.
                                format: int32
                                type: integer
                            type: object
                          type: array
                        source_type:
                          description: Specifies the connection source IP match type.
                            Can be any, local or external network.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        suffix_len:
                          description: '[#not-implemented-hide:]'
                          format: int32
                          type: integer
                        transport_protocol:
                          description: 'If non-empty, a transport protocol to consider
                            when determining a filter chain match.  This value will
                            be compared against the transport protocol of a new connection,
                            when  it''s detected by one of the listener filters.   Suggested
                            values include:   * ``raw_buffer`` - default, used when
                            no transport protocol is detected,  * ``tls`` - set by
                            :ref:`envoy.filters.listener.tls_inspector <config_listener_filters_tls_inspector>`    when
                            TLS protocol is detected.'
                          type: string
                      type: object
                    filters:
                      description: 'A list of individual network filters that make
                        up the filter chain for  connections established with the
                        listener. Order matters as the filters are  processed sequentially
                        as connection events happen. Note: If the filter  list is
                        empty, the connection will close by default.   For QUIC listeners,
                        network filters other than HTTP Connection Manager (HCM)  can
                        be created, but due to differences in the connection implementation
                        compared  to TCP, the onData() method will never be called.
                        Therefore, network filters  for QUIC listeners should only
                        expect to do work at the start of a new connection  (i.e.
                        in onNewConnection()). HCM must be the last (or only) filter
                        in the chain.'
                      items:
                        description: '[#next-free-field: 6]'
                        properties:
                          config_discovery:
                            description: Configuration source specifier for an extension
                              configuration discovery  service. In case of a failure
                              and without the default configuration, the  listener
                              closes the connections.
                            properties:
                              apply_default_config_without_warming:
                                description: Use the default config as the initial
                                  configuration without warming and  waiting for the
                                  first discovery response. Requires the default configuration  to
                                  be supplied.
                                type: boolean
                              config_source:
                                description: 'Configuration for :ref:`listeners <config_listeners>`,
                                  :ref:`clusters  <config_cluster_manager>`, :ref:`routes  <envoy_v3_api_msg_config.route.v3.RouteConfiguration>`,
                                  :ref:`endpoints  <arch_overview_service_discovery>`
                                  etc. may either be sourced from the  filesystem
                                  or from an xDS API source. Filesystem configs are
                                  watched with  inotify for updates.  [#next-free-field:
                                  9]'
                                properties:
                                  ads:
                                    description: When set, ADS will be used to fetch
                                      resources. The ADS API configuration  source
                                      in the bootstrap configuration is used.
                                    type: object
                                    x-kubernetes-preserve-unknown-fields: true
                                  api_config_source:
                                    description: API configuration source.
                                    properties:
                                      api_type:
                                        description: API type (gRPC, REST, delta gRPC)
                                        type: object
                                        x-kubernetes-preserve-unknown-fields: true
                                      cluster_names:
                                        description: 'Cluster names should be used
                                          only with REST. If > 1  cluster is defined,
                                          clusters will be cycled through if any kind
                                          of failure  occurs.   .. note::    The cluster
                                          with name ``cluster_name`` must be statically
                                          defined and its   type must not be ``EDS``.'
                                        items:
                                          type: string
                                        type: array
                                      config_validators:
                                        description: 'A list of config validators
                                          that will be executed when a new update
                                          is  received from the ApiConfigSource. Note
                                          that each validator handles a  specific
                                          xDS service type, and only the validators
                                          corresponding to the  type url (in ``:ref:
                                          DiscoveryResponse`` or ``:ref: DeltaDiscoveryResponse``)  will
                                          be invoked.  If the validator returns false
                                          or throws an exception, the config will
                                          be rejected by  the client, and a NACK will
                                          be sent.  [#extension-category: envoy.config.validators]'
                                        items:
                                          description: 'Message type for extension
                                            configuration.  [#next-major-version:
                                            revisit all existing typed_config that
                                            doesn''t use this wrapper.].'
                                          properties:
                                            name:
                                              description: The name of an extension.
                                                This is not used to select the extension,
                                                instead  it serves the role of an
                                                opaque identifier.
                                              type: string
                                            typed_config:
                                              description: The typed config for the
                                                extension. The type URL will be used
                                                to identify  the extension. In the
                                                case that the type URL is ``xds.type.v3.TypedStruct``  (or,
                                                for historical reasons, ``udpa.type.v1.TypedStruct``),
                                                the inner type  URL of ``TypedStruct``
                                                will be utilized. See the  :ref:`extension
                                                configuration overview  <config_overview_extension_configuration>`
                                                for further details.
                                              type: object
                                              x-kubernetes-preserve-unknown-fields: true
                                          type: object
                                        type: array
                                      grpc_services:
                                        description: Multiple gRPC services be provided
                                          for GRPC. If > 1 cluster is defined,  services
                                          will be cycled through if any kind of failure
                                          occurs.
                                        items:
                                          description: 'gRPC service configuration.
                                            This is used by :ref:`ApiConfigSource  <envoy_v3_api_msg_config.core.v3.ApiConfigSource>`
                                            and filter configurations.  [#next-free-field:
                                            7]'
                                          properties:
                                            envoy_grpc:
                                              description: Envoy's in-built gRPC client.  See
                                                the :ref:`gRPC services overview <arch_overview_grpc_services>`  documentation
                                                for discussion on gRPC client selection.
                                              type: object
                                              x-kubernetes-preserve-unknown-fields: true
                                            google_grpc:
                                              description: '`Google C++ gRPC client
                                                <https://github.com/grpc/grpc>`_  See
                                                the :ref:`gRPC services overview <arch_overview_grpc_services>`  documentation
                                                for discussion on gRPC client selection.'
                                              type: object
                                              x-kubernetes-preserve-unknown-fields: true
                                            initial_metadata:
                                              description: 'Additional metadata to
                                                include in streams initiated to the
                                                GrpcService. This can be used for  scenarios
                                                in which additional ad hoc authorization
                                                headers (e.g. ``x-foo-bar: baz-key``)
                                                are to  be injected. For more information,
                                                including details on header value
                                                syntax, see the  documentation on
                                                :ref:`custom request headers  <config_http_conn_man_headers_custom_request_headers>`.'
                                              items:
                                                description: Header name/value pair.
                                                properties:
                                                  key:
                                                    description: Header name.
                                                    type: string
                                                  raw_value:
                                                    description: Header value is encoded
                                                      as bytes which can support non-utf8
                                                      characters.  Only one of ``value``
                                                      or ``raw_value`` can be set.
                                                    format: byte
                                                    type: string
                                                  value:
                                                    description: Header value.   The
                                                      same :ref:`format specifier
                                                      <config_access_log_format>`
                                                      as used for  :ref:`HTTP access
                                                      logging <config_access_log>`
                                                      applies here, however  unknown
                                                      header values are replaced with
                                                      the empty string instead of
                                                      ``-``.  Header value is encoded
                                                      as string. This does not work
                                                      for non-utf8 characters.  Only
                                                      one of ``value`` or ``raw_value``
                                                      can be set.
                                                    type: string
                                                type: object
                                              type: array
                                            retry_policy:
                                              description: 'Optional default retry
                                                policy for RPCs or streams initiated
                                                toward this gRPC service.   If an
                                                async stream does not have a retry
                                                policy configured in its per‑stream
                                                options, this  policy is used as the
                                                default.   .. note::     This field
                                                is only applied by Envoy gRPC (``envoy_grpc``)
                                                clients. Google gRPC    (``google_grpc``)
                                                clients currently ignore this field.   If
                                                not specified, no default retry policy
                                                is applied at the client level and
                                                retries only occur  when explicitly
                                                configured in per‑stream options.'
                                              properties:
                                                host_selection_retry_max_attempts:
                                                  description: For details, see :ref:`host_selection_retry_max_attempts
                                                    <envoy_v3_api_field_config.route.v3.RetryPolicy.host_selection_retry_max_attempts>`.
                                                  format: int64
                                                  type: integer
                                                num_retries:
                                                  description: Specifies the allowed
                                                    number of retries. This parameter
                                                    is optional and  defaults to 1.
                                                  format: int32
                                                  type: integer
                                                retry_back_off:
                                                  description: Specifies parameters
                                                    that control :ref:`retry backoff
                                                    strategy <envoy_v3_api_msg_config.core.v3.BackoffStrategy>`.  This
                                                    parameter is optional, in which
                                                    case the default base interval
                                                    is 1000 milliseconds. The  default
                                                    maximum interval is 10 times the
                                                    base interval.
                                                  properties:
                                                    base_interval:
                                                      description: The base interval
                                                        to be used for the next back
                                                        off computation. It should  be
                                                        greater than zero and less
                                                        than or equal to :ref:`max_interval  <envoy_v3_api_field_config.core.v3.BackoffStrategy.max_interval>`.
                                                      type: string
                                                    max_interval:
                                                      description: Specifies the maximum
                                                        interval between retries.
                                                        This parameter is optional,  but
                                                        must be greater than or equal
                                                        to the :ref:`base_interval  <envoy_v3_api_field_config.core.v3.BackoffStrategy.base_interval>`
                                                        if set. The default  is 10
                                                        times the :ref:`base_interval  <envoy_v3_api_field_config.core.v3.BackoffStrategy.base_interval>`.
                                                      type: string
                                                  type: object
                                                retry_host_predicate:
                                                  description: For details, see :ref:`RetryHostPredicate
                                                    <envoy_v3_api_field_config.route.v3.RetryPolicy.retry_host_predicate>`.
                                                  items:
                                                    type: object
                                                    x-kubernetes-preserve-unknown-fields: true
                                                  type: array
                                                retry_on:
                                                  description: For details, see :ref:`retry_on
                                                    <envoy_v3_api_field_config.route.v3.RetryPolicy.retry_on>`.
                                                  type: string
                                                retry_priority:
                                                  description: For details, see :ref:`retry_priority
                                                    <envoy_v3_api_field_config.route.v3.RetryPolicy.retry_priority>`.
                                                  type: object
                                                  x-kubernetes-preserve-unknown-fields: true
                                              type: object
                                            timeout:
                                              description: The timeout for the gRPC
                                                request. This is the timeout for a
                                                specific  request.
                                              type: string
                                          type: object
                                        type: array
                                      rate_limit_settings:
                                        description: For GRPC APIs, the rate limit
                                          settings. If present, discovery requests
                                          made by Envoy will be  rate limited.
                                        properties:
                                          fill_rate:
                                            description: Rate at which tokens will
                                              be filled per second. If not set, a
                                              default fill rate of 10 tokens  per
                                              second will be used. The minimal fill
                                              rate is once per year. Lower  fill rates
                                              will be set to once per year.
                                            format: double
                                            type: number
                                          max_tokens:
                                            description: Maximum number of tokens
                                              to be used for rate limiting discovery
                                              request calls. If not set, a  default
                                              value of 100 will be used.
                                            format: int32
                                            type: integer
                                        type: object
                                      refresh_delay:
                                        description: For REST APIs, the delay between
                                          successive polls.
                                        type: string
                                      request_timeout:
                                        description: For REST APIs, the request timeout.
                                          If not set, a default value of 1s will be
                                          used.
                                        type: string
                                      set_node_on_first_message_only:
                                        description: Skip the node identifier in subsequent
                                          discovery requests for streaming gRPC config
                                          types.
                                        type: boolean
                                      transport_api_version:
                                        description: API version for xDS transport
                                          protocol. This describes the xDS gRPC/REST  endpoint
                                          and version of [Delta]DiscoveryRequest/Response
                                          used on the wire.
                                        enum:
                                        - AUTO
                                        - V2
                                        - V3
                                        type: string
                                    type: object
                                  authorities:
                                    description: Authorities that this config source
                                      may be used for. An authority specified in a
                                      xdstp:// URL  is resolved to a ``ConfigSource``
                                      prior to configuration fetch. This field provides
                                      the  association between authority name and
                                      configuration source.  [#not-implemented-hide:]
                                    items:
                                      type: object
                                      x-kubernetes-preserve-unknown-fields: true
                                    type: array
                                  initial_fetch_timeout:
                                    description: When this timeout is specified, Envoy
                                      will wait no longer than the specified time
                                      for first  config response on this xDS subscription
                                      during the :ref:`initialization process  <arch_overview_initialization>`.
                                      After reaching the timeout, Envoy will move
                                      to the next  initialization phase, even if the
                                      first config is not delivered yet. The timer
                                      is activated  when the xDS API subscription
                                      starts, and is disarmed on first config update
                                      or on error. 0  means no timeout - Envoy will
                                      wait indefinitely for the first xDS config (unless
                                      another  timeout applies). The default is 15s.
                                    type: string
                                  path:
                                    description: Deprecated in favor of ``path_config_source``.
                                      Use that field instead.
                                    type: string
                                  path_config_source:
                                    description: Local filesystem path configuration
                                      source.
                                    properties:
                                      path:
                                        description: 'Path on the filesystem to source
                                          and watch for configuration updates.  When
                                          sourcing configuration for a :ref:`secret
                                          <envoy_v3_api_msg_extensions.transport_sockets.tls.v3.Secret>`,  the
                                          certificate and key files are also watched
                                          for updates.   .. note::    The path to
                                          the source must exist at config load time.   ..
                                          note::     If ``watched_directory`` is *not*
                                          configured, Envoy will watch the file path
                                          for *moves*.    This is because in general
                                          only moves are atomic. The same method of
                                          swapping files as is    demonstrated in
                                          the :ref:`runtime documentation <config_runtime_symbolic_link_swap>`
                                          can be    used here also. If ``watched_directory``
                                          is configured, no watch will be placed directly
                                          on    this path. Instead, the configured
                                          ``watched_directory`` will be used to trigger
                                          reloads of    this path. This is required
                                          in certain deployment scenarios. See below
                                          for more information.'
                                        type: string
                                      watched_directory:
                                        description: 'If configured, this directory
                                          will be watched for *moves*. When an entry
                                          in this directory is  moved to, the ``path``
                                          will be reloaded. This is required in certain
                                          deployment scenarios.   Specifically, if
                                          trying to load an xDS resource using a  `Kubernetes
                                          ConfigMap <https://kubernetes.io/docs/concepts/configuration/configmap/>`_,
                                          the  following configuration might be used:  1.
                                          Store xds.yaml inside a ConfigMap.  2. Mount
                                          the ConfigMap to ``/config_map/xds``  3.
                                          Configure path ``/config_map/xds/xds.yaml``  4.
                                          Configure watched directory ``/config_map/xds``   The
                                          above configuration will ensure that Envoy
                                          watches the owning directory for moves which
                                          is  required due to how Kubernetes manages
                                          ConfigMap symbolic links during atomic updates.'
                                        properties:
                                          path:
                                            description: Directory path to watch.
                                            type: string
                                        type: object
                                    type: object
                                  resource_api_version:
                                    description: API version for xDS resources. This
                                      implies the type URLs that the client  will
                                      request for resources and the resource type
                                      that the client will in  turn expect to be delivered.
                                    enum:
                                    - AUTO
                                    - V2
                                    - V3
                                    type: string
                                  self:
                                    description: '[#not-implemented-hide:]  When set,
                                      the client will access the resources from the
                                      same server it got the  ConfigSource from, although
                                      not necessarily from the same stream. This is
                                      similar to the  :ref:`ads<envoy_v3_api_field.ConfigSource.ads>`
                                      field, except that the client may use a  different
                                      stream to the same server. As a result, this
                                      field can be used for things  like LRS that
                                      cannot be sent on an ADS stream. It can also
                                      be used to link from (e.g.)  LDS to RDS on the
                                      same server without requiring the management
                                      server to know its name  or required credentials.  [#next-major-version:
                                      In xDS v3, consider replacing the ads field
                                      with this one, since  this field can implicitly
                                      mean to use the same stream in the case where
                                      the ConfigSource  is provided via ADS and the
                                      specified data can also be obtained via ADS.]'
                                    properties:
                                      transport_api_version:
                                        description: API version for xDS transport
                                          protocol. This describes the xDS gRPC/REST  endpoint
                                          and version of [Delta]DiscoveryRequest/Response
                                          used on the wire.
                                        enum:
                                        - AUTO
                                        - V2
                                        - V3
                                        type: string
                                    type: object
                                type: object
                              default_config:
                                description: Optional default configuration to use
                                  as the initial configuration if  there is a failure
                                  to receive the initial extension configuration or
                                  if  ``apply_default_config_without_warming`` flag
                                  is set.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              type_urls:
                                description: A set of permitted extension type URLs
                                  for the type encoded inside of the  :ref:`TypedExtensionConfig
                                  <envoy_v3_api_msg_config.core.v3.TypedExtensionConfig>`.
                                  Extension  configuration updates are rejected if
                                  they do not match any type URL in the set.
                                items:
                                  type: string
                                type: array
                            type: object
                          name:
                            description: The name of the filter configuration.
                            type: string
                          typed_config:
                            description: 'Filter specific configuration which depends
                              on the filter being  instantiated. See the supported
                              filters for further documentation.  [#extension-category:
                              envoy.filters.network]'
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                        type: object
                      type: array
                    metadata:
                      description: Filter chain metadata.
                      properties:
                        filter_metadata:
                          additionalProperties:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          type: object
                        typed_filter_metadata:
                          additionalProperties:
                            description: 'The type in this field determines the type
                              of API listener. At present, the following  types are
                              supported:  envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                              (HTTP)  envoy.extensions.filters.network.http_connection_manager.v3.EnvoyMobileHttpConnectionManager
                              (HTTP)  [#next-major-version: In the v3 API, replace
                              this Any field with a oneof containing the  specific
                              config message for each type of API listener. We could
                              not do this in v2 because  it would have caused circular
                              dependencies for go protos: lds.proto depends on this
                              file,  and http_connection_manager.proto depends on
                              rds.proto, which is in the same directory as  lds.proto,
                              so lds.proto cannot depend on this file.]'
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          type: object
                      type: object
                    name:
                      description: 'The unique name (or empty) by which this filter
                        chain is known.   .. note::      :ref:`filter_chain_matcher      <envoy_v3_api_field_config.listener.v3.Listener.filter_chain_matcher>`      requires
                        that filter chains are uniquely named within a listener.'
                      type: string
                    transport_socket:
                      description: 'Optional custom transport socket implementation
                        to use for downstream connections.  To setup TLS, set a transport
                        socket with name ``envoy.transport_sockets.tls`` and  :ref:`DownstreamTlsContext
                        <envoy_v3_api_msg_extensions.transport_sockets.tls.v3.DownstreamTlsContext>`
                        in the ``typed_config``.  If no transport socket configuration
                        is specified, new connections  will be set up with plaintext.  [#extension-category:
                        envoy.transport_sockets.downstream]'
                      properties:
                        name:
                          description: The name of the transport socket to instantiate.
                            The name must match a supported transport  socket implementation.
                          type: string
                        typed_config:
                          description: Optional default configuration to use as the
                            initial configuration if  there is a failure to receive
                            the initial extension configuration or if  ``apply_default_config_without_warming``
                            flag is set.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    transport_socket_connect_timeout:
                      description: If present and nonzero, the amount of time to allow
                        incoming connections to complete any  transport socket negotiations.
                        If this expires before the transport reports connection  establishment,
                        the connection is summarily closed.
                      type: string
                    use_proxy_proto:
                      description: Whether the listener should expect a PROXY protocol
                        V1 header on new  connections. If this option is enabled,
                        the listener will assume that that  remote address of the
                        connection is the one specified in the header. Some  load
                        balancers including the AWS ELB support this option. If the
                        option is  absent or set to false, Envoy will use the physical
                        peer address of the  connection as the remote address.   This
                        field is deprecated. Add a  :ref:`PROXY protocol listener
                        filter <config_listener_filters_proxy_protocol>`  explicitly
                        instead.
                      type: boolean
                  type: object
                type: array
              freebind:
                description: Whether the listener should set the ``IP_FREEBIND`` socket
                  option. When this  flag is set to true, listeners can be bound to
                  an IP address that is not  configured on the system running Envoy.
                  When this flag is set to false, the  option ``IP_FREEBIND`` is disabled
                  on the socket. When this flag is not set  (default), the socket
                  is not modified, i.e. the option is neither enabled  nor disabled.
                type: boolean
              ignore_global_conn_limit:
                description: Whether the listener should limit connections based upon
                  the value of  :ref:`global_downstream_max_connections <config_overload_manager_limiting_connections>`.
                type: boolean
              internal_listener:
                description: 'Used to represent an internal listener which does not
                  listen on OSI L4 address but can be used by the  :ref:`envoy cluster
                  <envoy_v3_api_msg_config.cluster.v3.Cluster>` to create a user space
                  connection to.  The internal listener acts as a TCP listener. It
                  supports listener filters and network filter chains.  Upstream clusters
                  refer to the internal listeners by their :ref:`name  <envoy_v3_api_field_config.listener.v3.Listener.name>`.
                  :ref:`Address  <envoy_v3_api_field_config.listener.v3.Listener.address>`
                  must not be set on the internal listeners.   There are some limitations
                  that are derived from the implementation. The known limitations
                  include:   * :ref:`ConnectionBalanceConfig <envoy_v3_api_msg_config.listener.v3.Listener.ConnectionBalanceConfig>`
                  is not    allowed because both the cluster connection and the listener
                  connection must be owned by the same dispatcher.  * :ref:`tcp_backlog_size
                  <envoy_v3_api_field_config.listener.v3.Listener.tcp_backlog_size>`  *
                  :ref:`freebind <envoy_v3_api_field_config.listener.v3.Listener.freebind>`  *
                  :ref:`transparent <envoy_v3_api_field_config.listener.v3.Listener.transparent>`'
                type: object
                x-kubernetes-preserve-unknown-fields: true
              listener_filters:
                description: Listener filters have the opportunity to manipulate and
                  augment the connection metadata that  is used in connection filter
                  chain matching, for example. These filters are run before any in  :ref:`filter_chains
                  <envoy_v3_api_field_config.listener.v3.Listener.filter_chains>`.
                  Order matters as the  filters are processed sequentially right after
                  a socket has been accepted by the listener, and  before a connection
                  is created.  UDP Listener filters can be specified when the protocol
                  in the listener socket address in  :ref:`protocol <envoy_v3_api_field_config.core.v3.SocketAddress.protocol>`
                  is :ref:`UDP  <envoy_v3_api_enum_value_config.core.v3.SocketAddress.Protocol.UDP>`
                  and no  :ref:`quic_options <envoy_v3_api_field_config.listener.v3.UdpListenerConfig.quic_options>`
                  is specified in :ref:`udp_listener_config <envoy_v3_api_field_config.listener.v3.Listener.udp_listener_config>`.  QUIC
                  listener filters can be specified when :ref:`quic_options  <envoy_v3_api_field_config.listener.v3.UdpListenerConfig.quic_options>`
                  is  specified in :ref:`udp_listener_config <envoy_v3_api_field_config.listener.v3.Listener.udp_listener_config>`.  They
                  are processed sequentially right before connection creation. And
                  like TCP Listener filters, they can be used to manipulate the connection
                  metadata and socket. But the difference is that they can't be used
                  to pause connection creation.
                items:
                  description: '[#next-free-field: 6]'
                  properties:
                    config_discovery:
                      description: Configuration source specifier for an extension
                        configuration discovery  service. In case of a failure and
                        without the default configuration, the  listener closes the
                        connections.
                      properties:
                        apply_default_config_without_warming:
                          description: Use the default config as the initial configuration
                            without warming and  waiting for the first discovery response.
                            Requires the default configuration  to be supplied.
                          type: boolean
                        config_source:
                          description: 'Configuration for :ref:`listeners <config_listeners>`,
                            :ref:`clusters  <config_cluster_manager>`, :ref:`routes  <envoy_v3_api_msg_config.route.v3.RouteConfiguration>`,
                            :ref:`endpoints  <arch_overview_service_discovery>` etc.
                            may either be sourced from the  filesystem or from an
                            xDS API source. Filesystem configs are watched with  inotify
                            for updates.  [#next-free-field: 9]'
                          properties:
                            ads:
                              description: When set, ADS will be used to fetch resources.
                                The ADS API configuration  source in the bootstrap
                                configuration is used.
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            api_config_source:
                              description: API configuration source.
                              properties:
                                api_type:
                                  description: API type (gRPC, REST, delta gRPC)
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                cluster_names:
                                  description: 'Cluster names should be used only
                                    with REST. If > 1  cluster is defined, clusters
                                    will be cycled through if any kind of failure  occurs.   ..
                                    note::    The cluster with name ``cluster_name``
                                    must be statically defined and its   type must
                                    not be ``EDS``.'
                                  items:
                                    type: string
                                  type: array
                                config_validators:
                                  description: 'A list of config validators that will
                                    be executed when a new update is  received from
                                    the ApiConfigSource. Note that each validator
                                    handles a  specific xDS service type, and only
                                    the validators corresponding to the  type url
                                    (in ``:ref: DiscoveryResponse`` or ``:ref: DeltaDiscoveryResponse``)  will
                                    be invoked.  If the validator returns false or
                                    throws an exception, the config will be rejected
                                    by  the client, and a NACK will be sent.  [#extension-category:
                                    envoy.config.validators]'
                                  items:
                                    description: 'Message type for extension configuration.  [#next-major-version:
                                      revisit all existing typed_config that doesn''t
                                      use this wrapper.].'
                                    properties:
                                      name:
                                        description: The name of an extension. This
                                          is not used to select the extension, instead  it
                                          serves the role of an opaque identifier.
                                        type: string
                                      typed_config:
                                        description: The typed config for the extension.
                                          The type URL will be used to identify  the
                                          extension. In the case that the type URL
                                          is ``xds.type.v3.TypedStruct``  (or, for
                                          historical reasons, ``udpa.type.v1.TypedStruct``),
                                          the inner type  URL of ``TypedStruct`` will
                                          be utilized. See the  :ref:`extension configuration
                                          overview  <config_overview_extension_configuration>`
                                          for further details.
                                        type: object
                                        x-kubernetes-preserve-unknown-fields: true
                                    type: object
                                  type: array
                                grpc_services:
                                  description: Multiple gRPC services be provided
                                    for GRPC. If > 1 cluster is defined,  services
                                    will be cycled through if any kind of failure
                                    occurs.
                                  items:
                                    description: 'gRPC service configuration. This
                                      is used by :ref:`ApiConfigSource  <envoy_v3_api_msg_config.core.v3.ApiConfigSource>`
                                      and filter configurations.  [#next-free-field:
                                      7]'
                                    properties:
                                      envoy_grpc:
                                        description: Envoy's in-built gRPC client.  See
                                          the :ref:`gRPC services overview <arch_overview_grpc_services>`  documentation
                                          for discussion on gRPC client selection.
                                        type: object
                                        x-kubernetes-preserve-unknown-fields: true
                                      google_grpc:
                                        description: '`Google C++ gRPC client <https://github.com/grpc/grpc>`_  See
                                          the :ref:`gRPC services overview <arch_overview_grpc_services>`  documentation
                                          for discussion on gRPC client selection.'
                                        type: object
                                        x-kubernetes-preserve-unknown-fields: true
                                      initial_metadata:
                                        description: 'Additional metadata to include
                                          in streams initiated to the GrpcService.
                                          This can be used for  scenarios in which
                                          additional ad hoc authorization headers
                                          (e.g. ``x-foo-bar: baz-key``) are to  be
                                          injected. For more information, including
                                          details on header value syntax, see the  documentation
                                          on :ref:`custom request headers  <config_http_conn_man_headers_custom_request_headers>`.'
                                        items:
                                          description: Header name/value pair.
                                          properties:
                                            key:
                                              description: Header name.
                                              type: string
                                            raw_value:
                                              description: Header value is encoded
                                                as bytes which can support non-utf8
                                                characters.  Only one of ``value``
                                                or ``raw_value`` can be set.
                                              format: byte
                                              type: string
                                            value:
                                              description: Header value.   The same
                                                :ref:`format specifier <config_access_log_format>`
                                                as used for  :ref:`HTTP access logging
                                                <config_access_log>` applies here,
                                                however  unknown header values are
                                                replaced with the empty string instead
                                                of ``-``.  Header value is encoded
                                                as string. This does not work for
                                                non-utf8 characters.  Only one of
                                                ``value`` or ``raw_value`` can be
                                                set.
                                              type: string
                                          type: object
                                        type: array
                                      retry_policy:
                                        description: 'Optional default retry policy
                                          for RPCs or streams initiated toward this
                                          gRPC service.   If an async stream does
                                          not have a retry policy configured in its
                                          per‑stream options, this  policy is used
                                          as the default.   .. note::     This field
                                          is only applied by Envoy gRPC (``envoy_grpc``)
                                          clients. Google gRPC    (``google_grpc``)
                                          clients currently ignore this field.   If
                                          not specified, no default retry policy is
                                          applied at the client level and retries
                                          only occur  when explicitly configured in
                                          per‑stream options.'
                                        properties:
                                          host_selection_retry_max_attempts:
                                            description: For details, see :ref:`host_selection_retry_max_attempts
                                              <envoy_v3_api_field_config.route.v3.RetryPolicy.host_selection_retry_max_attempts>`.
                                            format: int64
                                            type: integer
                                          num_retries:
                                            description: Specifies the allowed number
                                              of retries. This parameter is optional
                                              and  defaults to 1.
                                            format: int32
                                            type: integer
                                          retry_back_off:
                                            description: Specifies parameters that
                                              control :ref:`retry backoff strategy
                                              <envoy_v3_api_msg_config.core.v3.BackoffStrategy>`.  This
                                              parameter is optional, in which case
                                              the default base interval is 1000 milliseconds.
                                              The  default maximum interval is 10
                                              times the base interval.
                                            properties:
                                              base_interval:
                                                description: The base interval to
                                                  be used for the next back off computation.
                                                  It should  be greater than zero
                                                  and less than or equal to :ref:`max_interval  <envoy_v3_api_field_config.core.v3.BackoffStrategy.max_interval>`.
                                                type: string
                                              max_interval:
                                                description: Specifies the maximum
                                                  interval between retries. This parameter
                                                  is optional,  but must be greater
                                                  than or equal to the :ref:`base_interval  <envoy_v3_api_field_config.core.v3.BackoffStrategy.base_interval>`
                                                  if set. The default  is 10 times
                                                  the :ref:`base_interval  <envoy_v3_api_field_config.core.v3.BackoffStrategy.base_interval>`.
                                                type: string
                                            type: object
                                          retry_host_predicate:
                                            description: For details, see :ref:`RetryHostPredicate
                                              <envoy_v3_api_field_config.route.v3.RetryPolicy.retry_host_predicate>`.
                                            items:
                                              type: object
                                              x-kubernetes-preserve-unknown-fields: true
                                            type: array
                                          retry_on:
                                            description: For details, see :ref:`retry_on
                                              <envoy_v3_api_field_config.route.v3.RetryPolicy.retry_on>`.
                                            type: string
                                          retry_priority:
                                            description: For details, see :ref:`retry_priority
                                              <envoy_v3_api_field_config.route.v3.RetryPolicy.retry_priority>`.
                                            type: object
                                            x-kubernetes-preserve-unknown-fields: true
                                        type: object
                                      timeout:
                                        description: The timeout for the gRPC request.
                                          This is the timeout for a specific  request.
                                        type: string
                                    type: object
                                  type: array
                                rate_limit_settings:
                                  description: For GRPC APIs, the rate limit settings.
                                    If present, discovery requests made by Envoy will
                                    be  rate limited.
                                  properties:
                                    fill_rate:
                                      description: Rate at which tokens will be filled
                                        per second. If not set, a default fill rate
                                        of 10 tokens  per second will be used. The
                                        minimal fill rate is once per year. Lower  fill
                                        rates will be set to once per year.
                                      format: double
                                      type: number
                                    max_tokens:
                                      description: Maximum number of tokens to be
                                        used for rate limiting discovery request calls.
                                        If not set, a  default value of 100 will be
                                        used.
                                      format: int32
                                      type: integer
                                  type: object
                                refresh_delay:
                                  description: For REST APIs, the delay between successive
                                    polls.
                                  type: string
                                request_timeout:
                                  description: For REST APIs, the request timeout.
                                    If not set, a default value of 1s will be used.
                                  type: string
                                set_node_on_first_message_only:
                                  description: Skip the node identifier in subsequent
                                    discovery requests for streaming gRPC config types.
                                  type: boolean
                                transport_api_version:
                                  description: API version for xDS transport protocol.
                                    This describes the xDS gRPC/REST  endpoint and
                                    version of [Delta]DiscoveryRequest/Response used
                                    on the wire.
                                  enum:
                                  - AUTO
                                  - V2
                                  - V3
                                  type: string
                              type: object
                            authorities:
                              description: Authorities that this config source may
                                be used for. An authority specified in a xdstp://
                                URL  is resolved to a ``ConfigSource`` prior to configuration
                                fetch. This field provides the  association between
                                authority name and configuration source.  [#not-implemented-hide:]
                              items:
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              type: array
                            initial_fetch_timeout:
                              description: When this timeout is specified, Envoy will
                                wait no longer than the specified time for first  config
                                response on this xDS subscription during the :ref:`initialization
                                process  <arch_overview_initialization>`. After reaching
                                the timeout, Envoy will move to the next  initialization
                                phase, even if the first config is not delivered yet.
                                The timer is activated  when the xDS API subscription
                                starts, and is disarmed on first config update or
                                on error. 0  means no timeout - Envoy will wait indefinitely
                                for the first xDS config (unless another  timeout
                                applies). The default is 15s.
                              type: string
                            path:
                              description: Deprecated in favor of ``path_config_source``.
                                Use that field instead.
                              type: string
                            path_config_source:
                              description: Local filesystem path configuration source.
                              properties:
                                path:
                                  description: 'Path on the filesystem to source and
                                    watch for configuration updates.  When sourcing
                                    configuration for a :ref:`secret <envoy_v3_api_msg_extensions.transport_sockets.tls.v3.Secret>`,  the
                                    certificate and key files are also watched for
                                    updates.   .. note::    The path to the source
                                    must exist at config load time.   .. note::     If
                                    ``watched_directory`` is *not* configured, Envoy
                                    will watch the file path for *moves*.    This
                                    is because in general only moves are atomic. The
                                    same method of swapping files as is    demonstrated
                                    in the :ref:`runtime documentation <config_runtime_symbolic_link_swap>`
                                    can be    used here also. If ``watched_directory``
                                    is configured, no watch will be placed directly
                                    on    this path. Instead, the configured ``watched_directory``
                                    will be used to trigger reloads of    this path.
                                    This is required in certain deployment scenarios.
                                    See below for more information.'
                                  type: string
                                watched_directory:
                                  description: 'If configured, this directory will
                                    be watched for *moves*. When an entry in this
                                    directory is  moved to, the ``path`` will be reloaded.
                                    This is required in certain deployment scenarios.   Specifically,
                                    if trying to load an xDS resource using a  `Kubernetes
                                    ConfigMap <https://kubernetes.io/docs/concepts/configuration/configmap/>`_,
                                    the  following configuration might be used:  1.
                                    Store xds.yaml inside a ConfigMap.  2. Mount the
                                    ConfigMap to ``/config_map/xds``  3. Configure
                                    path ``/config_map/xds/xds.yaml``  4. Configure
                                    watched directory ``/config_map/xds``   The above
                                    configuration will ensure that Envoy watches the
                                    owning directory for moves which is  required
                                    due to how Kubernetes manages ConfigMap symbolic
                                    links during atomic updates.'
                                  properties:
                                    path:
                                      description: Directory path to watch.
                                      type: string
                                  type: object
                              type: object
                            resource_api_version:
                              description: API version for xDS resources. This implies
                                the type URLs that the client  will request for resources
                                and the resource type that the client will in  turn
                                expect to be delivered.
                              enum:
                              - AUTO
                              - V2
                              - V3
                              type: string
                            self:
                              description: '[#not-implemented-hide:]  When set, the
                                client will access the resources from the same server
                                it got the  ConfigSource from, although not necessarily
                                from the same stream. This is similar to the  :ref:`ads<envoy_v3_api_field.ConfigSource.ads>`
                                field, except that the client may use a  different
                                stream to the same server. As a result, this field
                                can be used for things  like LRS that cannot be sent
                                on an ADS stream. It can also be used to link from
                                (e.g.)  LDS to RDS on the same server without requiring
                                the management server to know its name  or required
                                credentials.  [#next-major-version: In xDS v3, consider
                                replacing the ads field with this one, since  this
                                field can implicitly mean to use the same stream in
                                the case where the ConfigSource  is provided via ADS
                                and the specified data can also be obtained via ADS.]'
                              properties:
                                transport_api_version:
                                  description: API version for xDS transport protocol.
                                    This describes the xDS gRPC/REST  endpoint and
                                    version of [Delta]DiscoveryRequest/Response used
                                    on the wire.
                                  enum:
                                  - AUTO
                                  - V2
                                  - V3
                                  type: string
                              type: object
                          type: object
                        default_config:
                          description: Optional default configuration to use as the
                            initial configuration if  there is a failure to receive
                            the initial extension configuration or if  ``apply_default_config_without_warming``
                            flag is set.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        type_urls:
                          description: A set of permitted extension type URLs for
                            the type encoded inside of the  :ref:`TypedExtensionConfig
                            <envoy_v3_api_msg_config.core.v3.TypedExtensionConfig>`.
                            Extension  configuration updates are rejected if they
                            do not match any type URL in the set.
                          items:
                            type: string
                          type: array
                      type: object
                    filter_disabled:
                      description: Optional match predicate used to disable the filter.
                        The filter is enabled when this field is empty.  See :ref:`ListenerFilterChainMatchPredicate
                        <envoy_v3_api_msg_config.listener.v3.ListenerFilterChainMatchPredicate>`  for
                        further examples.
                      properties:
                        and_match:
                          description: A set that describes a logical AND. If all
                            members of the set match, the match configuration  matches.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        any_match:
                          description: The match configuration will always match.
                          type: boolean
                        destination_port_range:
                          description: Match destination port. Particularly, the match
                            evaluation must use the recovered local port if  the owning
                            listener filter is after :ref:`an original_dst listener
                            filter <config_listener_filters_original_dst>`.
                          properties:
                            end:
                              description: end of the range (exclusive)
                              format: int32
                              type: integer
                            start:
                              description: start of the range (inclusive)
                              format: int32
                              type: integer
                          type: object
                        not_match:
                          description: A negation match. The match configuration will
                            match if the negated match condition matches.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        or_match:
                          description: A set that describes a logical OR. If any member
                            of the set matches, the match configuration  matches.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    name:
                      description: The name of the filter configuration.
                      type: string
                    typed_config:
                      description: 'Filter specific configuration which depends on
                        the filter being  instantiated. See the supported filters
                        for further documentation.  [#extension-category: envoy.filters.listener,envoy.filters.udp_listener]'
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
              listener_filters_timeout:
                description: The timeout to wait for all listener filters to complete
                  operation. If the timeout is reached,  the accepted socket is closed
                  without a connection being created unless  ``continue_on_listener_filters_timeout``
                  is set to true. Specify 0 to disable the  timeout. If not specified,
                  a default timeout of 15s is used.
                type: string
              max_connections_to_accept_per_socket_event:
                description: 'The maximum number of connections to accept from the
                  kernel per socket  event. Envoy may decide to close these connections
                  after accepting them  from the kernel e.g. due to load shedding,
                  or other policies.  If there are more than max_connections_to_accept_per_socket_event  connections
                  pending accept, connections over this threshold will be  accepted
                  in later event loop iterations.  If no value is provided Envoy will
                  accept all connections pending accept  from the kernel.   .. note::    It
                  is recommended to lower this value for better overload management
                  and reduced per-event cost.   Setting it to 1 is a viable option
                  with no noticeable impact on performance.'
                format: int32
                type: integer
              metadata:
                description: Listener metadata.
                properties:
                  filter_metadata:
                    additionalProperties:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: object
                  typed_filter_metadata:
                    additionalProperties:
                      description: 'The type in this field determines the type of
                        API listener. At present, the following  types are supported:  envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                        (HTTP)  envoy.extensions.filters.network.http_connection_manager.v3.EnvoyMobileHttpConnectionManager
                        (HTTP)  [#next-major-version: In the v3 API, replace this
                        Any field with a oneof containing the  specific config message
                        for each type of API listener. We could not do this in v2
                        because  it would have caused circular dependencies for go
                        protos: lds.proto depends on this file,  and http_connection_manager.proto
                        depends on rds.proto, which is in the same directory as  lds.proto,
                        so lds.proto cannot depend on this file.]'
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: object
                type: object
              name:
                description: The unique name by which this listener is known. If no
                  name is provided,  Envoy will allocate an internal UUID for the
                  listener. If the listener is to be dynamically  updated or removed
                  via :ref:`LDS <config_listeners_lds>` a unique name must be provided.
                type: string
              per_connection_buffer_limit_bytes:
                description: Soft limit on size of the listener’s new connection read
                  and write buffers.  If unspecified, an implementation defined default
                  is applied (1MiB).
                format: int32
                type: integer
              reuse_port:
                description: Deprecated. Use ``enable_reuse_port`` instead.
                type: boolean
              socket_options:
                description: Additional socket options that may not be present in
                  Envoy source code or  precompiled binaries.  It is not allowed to
                  update the socket options for any existing address if  :ref:`enable_reuse_port
                  <envoy_v3_api_field_config.listener.v3.Listener.enable_reuse_port>`  is
                  ``false`` to avoid the conflict when creating new sockets for the
                  listener.
                items:
                  description: 'Generic socket option message. This would be used
                    to set socket options that  might not exist in upstream kernels
                    or precompiled Envoy binaries.   For example:   .. code-block::
                    json    {     "description": "support tcp keep alive",     "state":
                    0,     "level": 1,     "name": 9,     "int_value": 1,   }   1
                    means SOL_SOCKET and 9 means SO_KEEPALIVE on Linux.  With the
                    above configuration, `TCP Keep-Alives <https://www.freesoft.org/CIE/RFC/1122/114.htm>`_  can
                    be enabled in socket with Linux, which can be used in  :ref:`listener''s<envoy_v3_api_field_config.listener.v3.Listener.socket_options>`
                    or  :ref:`admin''s <envoy_v3_api_field_config.bootstrap.v3.Admin.socket_options>`
                    socket_options etc.   It should be noted that the name or level
                    may have different values on different platforms.  [#next-free-field:
                    8]'
                  properties:
                    buf_value:
                      description: Otherwise it's a byte buffer.
                      format: byte
                      type: string
                    description:
                      description: An optional name to give this socket option for
                        debugging, etc.  Uniqueness is not required and no special
                        meaning is assumed.
                      type: string
                    int_value:
                      description: Because many sockopts take an int value.
                      format: int64
                      type: integer
                    level:
                      description: Corresponding to the level value passed to setsockopt,
                        such as IPPROTO_TCP
                      format: int64
                      type: integer
                    name:
                      description: The numeric name as passed to setsockopt
                      format: int64
                      type: integer
                    state:
                      description: The state in which the option will be applied.
                        When used in BindConfig  STATE_PREBIND is currently the only
                        valid value.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type:
                      description: Apply the socket option to the specified `socket
                        type <https://linux.die.net/man/2/socket>`_.  If not specified,
                        the socket option will be applied to all socket types.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
              stat_prefix:
                description: Optional prefix to use on listener stats. If empty, the
                  stats will be rooted at  ``listener.<address as string>.``. If non-empty,
                  stats will be rooted at  ``listener.<stat_prefix>.``.
                type: string
              tcp_backlog_size:
                description: The maximum length a tcp listener's pending connections
                  queue can grow to. If no value is  provided net.core.somaxconn will
                  be used on Linux and 128 otherwise.
                format: int32
                type: integer
              tcp_fast_open_queue_length:
                description: Whether the listener should accept TCP Fast Open (TFO)
                  connections.  When this flag is set to a value greater than 0, the
                  option TCP_FASTOPEN is enabled on  the socket, with a queue length
                  of the specified size  (see `details in RFC7413 <https://tools.ietf.org/html/rfc7413#section-5.1>`_).  When
                  this flag is set to 0, the option TCP_FASTOPEN is disabled on the
                  socket.  When this flag is not set (default), the socket is not
                  modified,  i.e. the option is neither enabled nor disabled.   On
                  Linux, the net.ipv4.tcp_fastopen kernel parameter must include flag
                  0x2 to enable  TCP_FASTOPEN.  See `ip-sysctl.txt <https://www.kernel.org/doc/Documentation/networking/ip-sysctl.txt>`_.   On
                  macOS, only values of 0, 1, and unset are valid; other values may
                  result in an error.  To set the queue length on macOS, set the net.inet.tcp.fastopen_backlog
                  kernel parameter.
                format: int32
                type: integer
              tcp_keepalive:
                description: If set, TCP keepalive settings are configured for the
                  listener address and inherited by  additional addresses. If not
                  set, TCP keepalive settings are not configured for the  listener
                  address and additional addresses by default. See :ref:`tcp_keepalive
                  <envoy_v3_api_field_config.listener.v3.AdditionalAddress.tcp_keepalive>`  to
                  explicitly configure TCP keepalive settings for individual additional
                  addresses.
                properties:
                  keepalive_interval:
                    description: The number of seconds between keep-alive probes.
                      Default is to use the OS  level configuration (unless overridden,
                      Linux defaults to 75s.) Setting this to  ``0`` disables TCP
                      keepalive.
                    format: int32
                    type: integer
                  keepalive_probes:
                    description: Maximum number of keepalive probes to send without
                      response before deciding  the connection is dead. Default is
                      to use the OS level configuration (unless  overridden, Linux
                      defaults to 9.) Setting this to ``0`` disables TCP keepalive.
                    format: int32
                    type: integer
                  keepalive_time:
                    description: The number of seconds a connection needs to be idle
                      before keep-alive probes  start being sent. Default is to use
                      the OS level configuration (unless  overridden, Linux defaults
                      to 7200s (i.e., 2 hours.) Setting this to ``0`` disables  TCP
                      keepalive.
                    format: int32
                    type: integer
                type: object
              traffic_direction:
                description: Specifies the intended direction of the traffic relative
                  to the local Envoy.  This property is required on Windows for listeners
                  using the original destination filter,  see :ref:`Original Destination
                  <config_listener_filters_original_dst>`.
                enum:
                - UNSPECIFIED
                - INBOUND
                - OUTBOUND
                type: string
              transparent:
                description: Whether the listener should be set as a transparent socket.  When
                  this flag is set to true, connections can be redirected to the listener
                  using an  ``iptables`` ``TPROXY`` target, in which case the original
                  source and destination addresses and  ports are preserved on accepted
                  connections. This flag should be used in combination with  :ref:`an
                  original_dst <config_listener_filters_original_dst>` :ref:`listener
                  filter  <envoy_v3_api_field_config.listener.v3.Listener.listener_filters>`
                  to mark the connections' local addresses as  "restored." This can
                  be used to hand off each redirected connection to another listener  associated
                  with the connection's destination address. Direct connections to
                  the socket without  using ``TPROXY`` cannot be distinguished from
                  connections redirected using ``TPROXY`` and are  therefore treated
                  as if they were redirected.  When this flag is set to false, the
                  listener's socket is explicitly reset as non-transparent.  Setting
                  this flag requires Envoy to run with the ``CAP_NET_ADMIN`` capability.  When
                  this flag is not set (default), the socket is not modified, i.e.
                  the transparent option  is neither set nor reset.
                type: boolean
              udp_listener_config:
                description: If the protocol in the listener socket address in :ref:`protocol  <envoy_v3_api_field_config.core.v3.SocketAddress.protocol>`
                  is :ref:`UDP  <envoy_v3_api_enum_value_config.core.v3.SocketAddress.Protocol.UDP>`,
                  this field specifies UDP  listener specific configuration.
                properties:
                  downstream_socket_config:
                    description: UDP socket configuration for the listener. The default
                      for  :ref:`prefer_gro <envoy_v3_api_field_config.core.v3.UdpSocketConfig.prefer_gro>`
                      is false for  listener sockets. If receiving a large amount
                      of datagrams from a small number of sources, it  may be worthwhile
                      to enable this option after performance testing.
                    properties:
                      max_rx_datagram_size:
                        description: The maximum size of received UDP datagrams. Using
                          a larger size will cause Envoy to allocate  more memory
                          per socket. Received datagrams above this size will be dropped.
                          If not set  defaults to 1500 bytes.
                        format: int64
                        type: integer
                      prefer_gro:
                        description: Configures whether Generic Receive Offload (GRO)  <https://en.wikipedia.org/wiki/Large_receive_offload>_
                          is preferred when reading from the  UDP socket. The default
                          is context dependent and is documented where UdpSocketConfig
                          is used.  This option affects performance but not functionality.
                          If GRO is not supported by the operating  system, non-GRO
                          receive will be used.
                        type: boolean
                    type: object
                  quic_options:
                    description: Configuration for QUIC protocol. If empty, QUIC will
                      not be enabled on this listener. Set  to the default object
                      to enable QUIC without modifying any additional options.
                    properties:
                      connection_debug_visitor_config:
                        description: 'Configure which implementation of ``quic::QuicConnectionDebugVisitor``
                          to be used for this listener.  If not specified, no debug
                          visitor will be attached to connections.  [#extension-category:
                          envoy.quic.connection_debug_visitor]'
                        properties:
                          name:
                            description: The name of an extension. This is not used
                              to select the extension, instead  it serves the role
                              of an opaque identifier.
                            type: string
                          typed_config:
                            description: The typed config for the extension. The type
                              URL will be used to identify  the extension. In the
                              case that the type URL is ``xds.type.v3.TypedStruct``  (or,
                              for historical reasons, ``udpa.type.v1.TypedStruct``),
                              the inner type  URL of ``TypedStruct`` will be utilized.
                              See the  :ref:`extension configuration overview  <config_overview_extension_configuration>`
                              for further details.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                        type: object
                      connection_id_generator_config:
                        description: 'Config which implementation of ``quic::ConnectionIdGeneratorInterface``
                          to be used for this listener.  If not specified the :ref:`default
                          one configured by <envoy_v3_api_msg_extensions.quic.connection_id_generator.v3.DeterministicConnectionIdGeneratorConfig>`
                          will be used.  [#extension-category: envoy.quic.connection_id_generator]'
                        properties:
                          name:
                            description: The name of an extension. This is not used
                              to select the extension, instead  it serves the role
                              of an opaque identifier.
                            type: string
                          typed_config:
                            description: The typed config for the extension. The type
                              URL will be used to identify  the extension. In the
                              case that the type URL is ``xds.type.v3.TypedStruct``  (or,
                              for historical reasons, ``udpa.type.v1.TypedStruct``),
                              the inner type  URL of ``TypedStruct`` will be utilized.
                              See the  :ref:`extension configuration overview  <config_overview_extension_configuration>`
                              for further details.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                        type: object
                      crypto_handshake_timeout:
                        description: Connection timeout in milliseconds before the
                          crypto handshake is finished.   If it is less than 5000ms,
                          Envoy will use 5000ms. 20000ms if not specified.
                        type: string
                      crypto_stream_config:
                        description: 'Configure which implementation of ``quic::QuicCryptoClientStreamBase``
                          to be used for this listener.  If not specified the :ref:`QUICHE
                          default one configured by <envoy_v3_api_msg_extensions.quic.crypto_stream.v3.CryptoServerStreamConfig>`
                          will be used.  [#extension-category: envoy.quic.server.crypto_stream]'
                        properties:
                          name:
                            description: The name of an extension. This is not used
                              to select the extension, instead  it serves the role
                              of an opaque identifier.
                            type: string
                          typed_config:
                            description: The typed config for the extension. The type
                              URL will be used to identify  the extension. In the
                              case that the type URL is ``xds.type.v3.TypedStruct``  (or,
                              for historical reasons, ``udpa.type.v1.TypedStruct``),
                              the inner type  URL of ``TypedStruct`` will be utilized.
                              See the  :ref:`extension configuration overview  <config_overview_extension_configuration>`
                              for further details.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                        type: object
                      enabled:
                        description: Runtime flag that controls whether the listener
                          is enabled or not. If not specified, defaults  to enabled.
                        properties:
                          default_value:
                            description: Default value if runtime value is not available.
                            type: boolean
                          runtime_key:
                            description: Runtime key to get value for comparison.
                              This value is used if defined. The boolean value must  be
                              represented via its  `canonical JSON encoding <https://developers.google.com/protocol-buffers/docs/proto3#json>`_.
                            type: string
                        type: object
                      idle_timeout:
                        description: Maximum number of milliseconds that connection
                          will be alive when there is  no network activity.   If it
                          is less than 1ms, Envoy will use 1ms. 300000ms if not specified.
                        type: string
                      max_sessions_per_event_loop:
                        description: Maximum number of QUIC sessions to create per
                          event loop.  If not specified, the default value is 16.  This
                          is an equivalent of the TCP listener option  max_connections_to_accept_per_socket_event.
                        format: int32
                        type: integer
                      packets_to_read_to_connection_count_ratio:
                        description: A multiplier to number of connections which is
                          used to determine how many packets to read per  event loop.
                          A reasonable number should allow the listener to process
                          enough payload but not  starve TCP and other UDP sockets
                          and also prevent long event loop duration.  The default
                          value is 32. This means if there are N QUIC connections,
                          the total number of  packets to read in each read event
                          will be 32 * N.  The actual number of packets to read in
                          total by the UDP listener is also  bound by 6000, regardless
                          of this field or how many connections there are.
                        format: int32
                        type: integer
                      proof_source_config:
                        description: 'Configure which implementation of ``quic::ProofSource``
                          to be used for this listener.  If not specified the :ref:`default
                          one configured by <envoy_v3_api_msg_extensions.quic.proof_source.v3.ProofSourceConfig>`
                          will be used.  [#extension-category: envoy.quic.proof_source]'
                        properties:
                          name:
                            description: The name of an extension. This is not used
                              to select the extension, instead  it serves the role
                              of an opaque identifier.
                            type: string
                          typed_config:
                            description: The typed config for the extension. The type
                              URL will be used to identify  the extension. In the
                              case that the type URL is ``xds.type.v3.TypedStruct``  (or,
                              for historical reasons, ``udpa.type.v1.TypedStruct``),
                              the inner type  URL of ``TypedStruct`` will be utilized.
                              See the  :ref:`extension configuration overview  <config_overview_extension_configuration>`
                              for further details.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                        type: object
                      quic_protocol_options:
                        description: 'QUIC protocol options which apply to both downstream
                          and upstream connections.  [#next-free-field: 12]'
                        properties:
                          client_connection_options:
                            description: A comma-separated list of strings representing
                              QUIC client connection options defined in  `QUICHE <https://github.com/google/quiche/blob/main/quiche/quic/core/crypto/crypto_protocol.h>`_
                              and to be sent by upstream connections.
                            type: string
                          client_packet_writer:
                            description: 'A customized UDP socket and a QUIC packet
                              writer using the socket for  client connections. i.e.
                              Mobile uses its own implementation to interact  with
                              platform socket APIs.  If not present, the default platform-independent
                              socket and writer will be used.  [#extension-category:
                              envoy.quic.client_packet_writer]'
                            properties:
                              name:
                                description: The name of an extension. This is not
                                  used to select the extension, instead  it serves
                                  the role of an opaque identifier.
                                type: string
                              typed_config:
                                description: The typed config for the extension. The
                                  type URL will be used to identify  the extension.
                                  In the case that the type URL is ``xds.type.v3.TypedStruct``  (or,
                                  for historical reasons, ``udpa.type.v1.TypedStruct``),
                                  the inner type  URL of ``TypedStruct`` will be utilized.
                                  See the  :ref:`extension configuration overview  <config_overview_extension_configuration>`
                                  for further details.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                            type: object
                          connection_keepalive:
                            description: Probes the peer at the configured interval
                              to solicit traffic, i.e. ``ACK`` or ``PATH_RESPONSE``,
                              from the peer to push back connection idle timeout.  If
                              absent, use the default keepalive behavior of which
                              a client connection sends ``PING``s every ``15s``, and
                              a server connection doesn't do anything.
                            properties:
                              initial_interval:
                                description: The interval to send the first few keep-alive
                                  probing packets to prevent connection from hitting
                                  the idle timeout. Subsequent probes will be sent,
                                  each one with an interval exponentially longer than
                                  previous one, till it reaches :ref:`max_interval
                                  <envoy_v3_api_field_config.core.v3.QuicKeepAliveSettings.max_interval>`.
                                  And the probes afterwards will always use :ref:`max_interval
                                  <envoy_v3_api_field_config.core.v3.QuicKeepAliveSettings.max_interval>`.   The
                                  value should be smaller than :ref:`connection idle_timeout
                                  <envoy_v3_api_field_config.listener.v3.QuicProtocolOptions.idle_timeout>`
                                  to prevent idle timeout and smaller than max_interval
                                  to take effect.   If absent, disable keepalive probing
                                  for a server connection. For a client connection,
                                  if :ref:`max_interval <envoy_v3_api_field_config.core.v3.QuicKeepAliveSettings.max_interval>`
                                  is zero, do not keepalive, otherwise use max_interval
                                  or QUICHE default to probe all the time.
                                type: string
                              max_interval:
                                description: The max interval for a connection to
                                  send keep-alive probing packets (with ``PING`` or
                                  ``PATH_RESPONSE``). The value should be smaller
                                  than :ref:`connection idle_timeout <envoy_v3_api_field_config.listener.v3.QuicProtocolOptions.idle_timeout>`
                                  to prevent idle timeout while not less than ``1s``
                                  to avoid throttling the connection or flooding the
                                  peer with probes.   If :ref:`initial_interval <envoy_v3_api_field_config.core.v3.QuicKeepAliveSettings.initial_interval>`
                                  is absent or zero, a client connection will use
                                  this value to start probing.   If zero, disable
                                  keepalive probing.  If absent, use the QUICHE default
                                  interval to probe.
                                type: string
                            type: object
                          connection_migration:
                            description: Enable QUIC `connection migration  <https://datatracker.ietf.org/doc/html/rfc9000#name-connection-migration>`  to
                              a different network interface when the current network
                              is degrading or  has become bad.  In order to use a
                              different network interface other than the platform's
                              default one,  a customized :ref:`client_packet_writer
                              <envoy_v3_api_field_config.core.v3.QuicProtocolOptions.client_packet_writer>`
                              needs to be configured to  create UDP sockets on non-default
                              networks.  Only takes effect when runtime key ``envoy.reloadable_features.use_migration_in_quiche``
                              is true.  If absent, the feature will be disabled.  [#not-implemented-hide:]
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          connection_options:
                            description: A comma-separated list of strings representing
                              QUIC connection options defined in  `QUICHE <https://github.com/google/quiche/blob/main/quiche/quic/core/crypto/crypto_protocol.h>`_
                              and to be sent by upstream connections.
                            type: string
                          idle_network_timeout:
                            description: The duration that a QUIC connection stays
                              idle before it closes itself. If this field is not present,
                              QUICHE  default ``600s`` will be applied.  For internal
                              corporate network, a long timeout is often fine.  But
                              for client facing network, ``30s`` is usually a good
                              choice.  Do not add an upper bound here. A long idle
                              timeout is useful for maintaining warm connections at
                              non-front-line proxy for low QPS services.
                            type: string
                          initial_connection_window_size:
                            description: 'Similar to ``initial_stream_window_size``,
                              but for connection-level  flow-control. Valid values
                              range from ``1`` to ``25165824`` (``24MB``, maximum
                              supported by QUICHE) and defaults  to ``25165824`` (``24
                              * 1024 * 1024``).   .. note::     ``16384`` (``2^14``)
                              is the minimum window size supported in Google QUIC.
                              We only support increasing the default    window size
                              now, so it''s also the minimum.'
                            format: int32
                            type: integer
                          initial_stream_window_size:
                            description: '`Initial stream-level flow-control receive
                              window  <https://tools.ietf.org/html/draft-ietf-quic-transport-34#section-4.1>`_
                              size. Valid values range from  ``1`` to ``16777216``
                              (``2^24``, maximum supported by QUICHE) and defaults
                              to ``16777216`` (``16 * 1024 * 1024``).   .. note::     ``16384``
                              (``2^14``) is the minimum window size supported in Google
                              QUIC. If configured smaller than it, we will use    ``16384``
                              instead. QUICHE IETF QUIC implementation supports ``1``
                              byte window. We only support increasing the default    window
                              size now, so it''s also the minimum.   This field also
                              acts as a soft limit on the number of bytes Envoy will
                              buffer per-stream in the  QUIC stream send and receive
                              buffers. Once the buffer reaches this pointer, watermark
                              callbacks will fire to  stop the flow of data to the
                              stream buffers.'
                            format: int32
                            type: integer
                          max_concurrent_streams:
                            description: Maximum number of streams that the client
                              can negotiate per connection. ``100``  if not specified.
                            format: int32
                            type: integer
                          max_packet_length:
                            description: Maximum packet length for QUIC connections.
                              It refers to the largest size of a QUIC packet that
                              can be transmitted over the connection.  If not specified,
                              one of the `default values in QUICHE <https://github.com/google/quiche/blob/main/quiche/quic/core/quic_constants.h>`_
                              is used.
                            format: int64
                            type: integer
                          num_timeouts_to_trigger_port_migration:
                            description: The number of timeouts that can occur before
                              port migration is triggered for QUIC clients.  This
                              defaults to ``4``. If set to ``0``, port migration will
                              not occur on path degrading.  Timeout here refers to
                              QUIC internal path degrading timeout mechanism, such
                              as ``PTO``.  This has no effect on server sessions.
                            format: int32
                            type: integer
                        type: object
                      reject_new_connections:
                        description: If true, the listener will reject connection-establishing
                          packets at the  QUIC layer by replying with an empty version
                          negotiation packet to the  client.
                        type: boolean
                      save_cmsg_config:
                        description: Configure a type of UDP cmsg to pass to listener
                          filters via QuicReceivedPacket.  Both level and type must
                          be specified for cmsg to be saved.  Cmsg may be truncated
                          or omitted if expected size is not set.  If not specified,
                          no cmsg will be saved to QuicReceivedPacket.
                        items:
                          description: Configuration for socket cmsg headers.  See
                            `:ref:CMSG <https://man7.org/linux/man-pages/man3/cmsg.3.html>`_
                            for further information.
                          properties:
                            expected_size:
                              description: Expected size of cmsg value. Default is
                                zero.
                              format: int32
                              type: integer
                            level:
                              description: cmsg level. Default is unset.
                              format: int32
                              type: integer
                            type:
                              description: cmsg type. Default is unset.
                              format: int32
                              type: integer
                          type: object
                        type: array
                      send_disable_active_migration:
                        description: Configure the server to send transport parameter
                          `disable_active_migration <https://www.rfc-editor.org/rfc/rfc9000#section-18.2-4.30.1>`_.  Defaults
                          to false (do not send this transport parameter).
                        type: boolean
                      server_preferred_address_config:
                        description: 'Configure the server''s preferred address to
                          advertise so that client can migrate to it. See :ref:`example
                          <envoy_v3_api_msg_extensions.quic.server_preferred_address.v3.FixedServerPreferredAddressConfig>`
                          which configures a pair of v4 and v6 preferred addresses.  The
                          current QUICHE implementation will advertise only one of
                          the preferred IPv4 and IPv6 addresses based on the address
                          family the client initially connects with.  If not specified,
                          Envoy will not advertise any server''s preferred address.  [#extension-category:
                          envoy.quic.server_preferred_address]'
                        properties:
                          name:
                            description: The name of an extension. This is not used
                              to select the extension, instead  it serves the role
                              of an opaque identifier.
                            type: string
                          typed_config:
                            description: The typed config for the extension. The type
                              URL will be used to identify  the extension. In the
                              case that the type URL is ``xds.type.v3.TypedStruct``  (or,
                              for historical reasons, ``udpa.type.v1.TypedStruct``),
                              the inner type  URL of ``TypedStruct`` will be utilized.
                              See the  :ref:`extension configuration overview  <config_overview_extension_configuration>`
                              for further details.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                        type: object
                    type: object
                  udp_packet_packet_writer_config:
                    description: 'Configuration for the UDP packet writer. If empty,
                      HTTP/3 will use GSO if available  (:ref:`UdpDefaultWriterFactory
                      <envoy_v3_api_msg_extensions.udp_packet_writer.v3.UdpGsoBatchWriterFactory>`)  or
                      the default kernel sendmsg if not,  (:ref:`UdpDefaultWriterFactory
                      <envoy_v3_api_msg_extensions.udp_packet_writer.v3.UdpDefaultWriterFactory>`)  and
                      raw UDP will use kernel sendmsg.  [#extension-category: envoy.udp_packet_writer]'
                    properties:
                      name:
                        description: The name of an extension. This is not used to
                          select the extension, instead  it serves the role of an
                          opaque identifier.
                        type: string
                      typed_config:
                        description: The typed config for the extension. The type
                          URL will be used to identify  the extension. In the case
                          that the type URL is ``xds.type.v3.TypedStruct``  (or, for
                          historical reasons, ``udpa.type.v1.TypedStruct``), the inner
                          type  URL of ``TypedStruct`` will be utilized. See the  :ref:`extension
                          configuration overview  <config_overview_extension_configuration>`
                          for further details.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                type: object
              use_original_dst:
                description: If a connection is redirected using ``iptables``, the
                  port on which the proxy  receives it might be different from the
                  original destination address. When this flag is set to  true, the
                  listener hands off redirected connections to the listener associated
                  with the  original destination address. If there is no listener
                  associated with the original destination  address, the connection
                  is handled by the listener that receives it. Defaults to false.
                type: boolean
            type: object
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
          targetRef:
            description: Reference to the EnvoyProxy CR that this resource is assigned
              to.
            properties:
              name:
                description: Name of the EnvoyProxy CR.
                type: string
              namespace:
                description: Namespace of the EnvoyProxy CR. Defaults to the namespace
                  of this resource.
                type: string
            required:
            - name
            type: object
        required:
        - targetRef
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
